# A股价格监控系统 — 需求与方案说明书

> **这份文档的用途**：把"需求"从 AI 的会话记忆里搬到项目文件里。
> 任何一天、换任何一台电脑、换任何一个 AI 或开发者，读完它就能直接接着干活，
> **你不需要重新讲一遍需求**。
>
> 最后更新：2026-09-20（新增 主力博弈引擎 / S2 中长线关键位置引擎 / F4 综合研判 / 大盘驾驶舱；持仓详情页「只留结论」；持仓清单取消成本/股数记账、删除持仓汇总卡死代码；V4/V5 移出计划）

---

## 一、一句话需求

> 盯住我关心的几只 A 股，价格到了我设定的条件就用**微信**叫我一声；
> 我能在**手机上**随时改要看哪只、看什么价、开还是关。

---

## 二、功能需求清单

### 2.1 已实现（当前线上运行中）

| 编号 | 需求 | 实现状态 | 所在位置 |
|---|---|---|---|
| F1 | 按股票名称/代码/拼音添加股票 | ✅ | 控制台 |
| F2 | 对单只股票开启 / 暂停监控 | ✅ | 控制台 |
| F3 | 删除股票 | ✅ | 控制台 |
| F4 | 条件提醒：等于 / 大于 / 大于等于 / 小于 / 小于等于 | ✅ | 云端脚本 |
| F5 | 微信消息通知 | ✅ | Server酱 服务号 |
| F6 | 实时价格展示（红涨绿跌） | ✅ | 控制台 |
| F7 | 只在 A 股交易时段监控：9:30-11:30、13:00-15:00 | ✅ | 云端脚本 |
| F8 | 防刷屏：只在"不满足→满足"那一刻提醒 + 30 分钟冷却 | ✅ | 云端脚本 |
| F9 | 电脑关机也照常监控（跑在 GitHub 免费云端） | ✅ | GitHub Actions |
| F10 | 删除记录归档：保留只读条件 + 首次触发价/时间 + 触发后至今涨跌幅 + 已删除天数 | ✅ | 控制台 |
| F11 | 未触发过的记录默认隐藏 | ✅ | 控制台 |
| F12 | 恢复监控 / 彻底移除记录 | ✅ | 控制台 |
| F13 | 显示"已监控 N 天" | ✅ | 控制台 |
| F14 | **A股恐贪情绪指数**：0-100，越大越贪婪（市场过热），越小越恐惧（市场冰冷） | ✅ | 控制台 + 云端 |
| F15 | 恐贪值**盘中实时变化**（盘中每 60 秒自动刷新） | ✅ | 控制台 |
| F16 | **每日收盘记录一个值，永久储存**（写入后不再变动） | ✅ | 云端 fng.js |
| F17 | 按周期看每日恐贪值曲线：7天 / 30天 / 90天 / 6个月 / 1年 / 2年 / 全部 | ✅ | 控制台 |
| F18 | 5 个分项拆解（趋势动量 / 波动率 / 量能 / 融资余额动能 / 融资净买入强度） | ✅ | 控制台 |
| F19 | 今日涨跌家数（参考，不计入指数） | ✅ | 控制台 |
| F20 | 入口卡**吸顶**：首页恐贪卡钉在顶部，不随股票监控列表上下滚动 | ✅ | 控制台 |
| F21 | **分阶段操作建议**：按当前情绪阶段给「后续走势预判 + 该做的 / 别做的」，拒绝"观望为主"式空话 | ✅ | 控制台 |
| F22 | 走势图**左右滑动**查看任意交易日节点（显示日期 + 数值 + 所处分区） | ✅ | 控制台 |
| F23 | **透视模式**：看清散户在干什么（割肉 / 追高接筹码 / 抄底）、主力在干什么（建仓 / 拉升 / 派发 / 锁仓），给出「你该站哪边、怎么操作」 | ✅ | 控制台 + 云端 chips.js |
| F24 | **5 日线连续站上标识**：日线开盘与收盘都高于当日 5 日线才算站上；连续 ≥2 天提示卖出、1 天只标天数、0 天不显示 | ✅ | 控制台（SignalCore.ma5Streak） |
| F24b | **同上口径的微信推送**：持仓股的这只票连续 ≥2 天开收盘站上 5 日线 → 进当日「【卖出/离场提醒】」推送（**只对持仓股发**；只监控还没买的票不收，避免以为自己持有） | ✅（规则 R2，代码已就绪） | 云端 signals.js + signal-core.js |
| F25 | 持仓卡 / 推荐卡显示**实时价 + 当日涨跌**（推荐卡另给「较推荐日」涨跌） | ✅ | 控制台 |
| F26 | 持仓清单**只记录代码与名称、不记成本/股数、不算盈亏**，故**没有**基于盈亏的主动提醒（条件提醒需加入监控清单；卖出提醒只来自 R2 的 5 日线连站） | ✅（2026-09-20 明确口径，非缺陷） | — |

### 2.2 恐贪指数 · 口径说明（重要，别改错）

> **它是什么**：把 5 个能反映"大盘 + 散户 + 资金"冷热的指标，各自换算成
> "在**过去 252 个交易日**里的百分位（0-100）"，再按权重加权求和。
> 所以它表达的是**相对自身历史的冷热位置**，50 分 ≈ 近一年中位水平。
> **它不是官方指数，是自建合成指标**，不能作为交易依据。

| 分项 | 权重 | 原始值定义 | 方向 | 数据源 |
|---|---|---|---|---|
| 趋势动量 | 20% | 中证全指 收盘 / MA20 − 1 | 越高越贪婪 | 腾讯日线 |
| 波动率 | 15% | 中证全指 20 日对数收益年化波动率 | **反向**（波动越大越恐慌） | 腾讯日线 |
| 量能热度 | 15% | 5 日均量 / 20 日均量 − 1 | 越高越贪婪 | 腾讯日线 |
| 融资余额动能 | 25% | 融资余额近 20 个交易日变化率 | 越高越贪婪 | 东财两融 |
| 融资净买入强度 | 25% | 近 5 日融资净买入合计 / 流通市值 | 越高越贪婪 | 东财两融 |

**为什么是这 5 项**：选型的第一原则是"**必须能回溯 3 年**"，否则"2 年/全部"曲线会缺一段。
实测排除了两类数据：
- **涨停家数 / 跌停家数**：东财接口只保留约 1 个月历史（`date` 参数早于约一个月就返回 0），无法回溯。
- **北向资金**：2024 年起已停止实时披露净流入（接口返回 null），故不纳入。

**为什么用中证全指而不是上证指数**：中证全指覆盖沪深全部 A 股，最接近"大盘整体"，能反映散户聚集的中小盘。

**两融为什么滞后**：两融数据是 T+1 披露的，所以某天的分值统一使用"**日期严格早于当天**的最近一条两融记录"，
保证盘中算的和收盘算的口径完全一致。

**归一化为什么用滚动百分位**：这样每个历史值只依赖"截至当天"的数据，
**写进存档后永远不会变**（无前视），回看曲线不会漂移。

**盘中实时怎么算**：盘中用腾讯日线当天那根（含实时价），量能按**日内成交节奏曲线**折算成全天估算值
（A股成交前重后也重，线性折算会低估上午的量）；两融用存档里的最新快照。收盘后由云端定稿。

**经验证的有效性**（2022-02 ~ 2026-09，1120 个交易日）：恐惧组（<40，380 天）之后 60 日平均 **+4.02%**，
中性组 +0.63%，贪婪组（≥60，383 天）**−0.67%** —— 单调且方向正确；
抓到了 2022 年 4 月底部（后 20 日 +7~10%）与 2026-05-13 的高点（后 20 日 −7.68%）。

**分阶段操作建议（F21）是怎么给的**：建议与"当前所处阶段"强绑定，每档都给三样东西——
① **后续走势预判**（带实证样本量与后 60 日平均收益）；② **该做的**；③ **别做的**。
五档内容互不相同，且刻意避免"观望为主"这类放在任何阶段都成立的空话：

| 阶段 | 预判要点 | 该做 | 别做 |
|---|---|---|---|
| 极度恐惧 | 冰冷，但历史上后 60 日平均 +4.0%；**持股体验最差 ≈ 离回暖最近** | 资金分 3~4 份分批建仓；基本面没坏就扛住 | **不要割肉**；不要因恐慌清仓 |
| 恐惧 | 后 60 日仍为正，但中间有反复 | 先建到半仓，留加仓空间 | 不要等"跌够"；不要一次打光子弹 |
| 中性 | 接近随机，情绪不提供方向 | 按原计划执行，重选股与纪律 | 不要追涨杀跌（信号最弱、最容易两头挨打） |
| 贪婪 | 后 60 日平均 −0.7%，回撤来得快 | 逐步兑现浮盈，降低仓位 | 不要加杠杆；不要"再赚一波就走" |
| 极度贪婪 | 最热、最热闹、最容易套人 | **减仓**（非清仓）+ 止盈落袋 + 留现金 | **不要追高**（尤其"看别人赚钱才进场"）；不要满仓过节 |

口径以 `FNGCore.adviceOf(v)` 为准（单一真源，控制台无法各写一份）。

**走势图滑动（F22）**：曲线支持左右滑动/拖动，按最近交易日**吸附**到节点，画竖直指示线 + 节点圆点 + 图上标签，
并在图上方的读数条显示「日期 · 数值 · 所在分区」。触摸交互用 `touch-action:pan-y` +
横向位移判定，保证纵向翻页滚动不被抢走。

### 2.3 恐贪指数 · 上线与运维状态（2026-09-16 已上线）

| 项 | 状态 |
|---|---|
| 控制台页面 | ✅ 已上线 `https://stock-alert-console.app.workbuddy.host/`（根路径自动跳 console.html） |
| 云端每日记录 | ✅ GitHub Actions `fng-daily`，每交易日北京 **16:05 定稿** + **22:05 刷两融** |
| 存档位置 | 仓库根目录 `fng-history.json`（750 个交易日滚动保留） |
| 归档读取 | ① GitHub API（需 token）→ ② **jsDelivr CDN**（免鉴权、国内可达）→ ③ raw（国内常不可达，置于最后）；每源 8–9s 超时 |
| 盘中数据 | 腾讯 `web.ifzq.gtimg.cn`（带 CORS，浏览器可直连），盘中每 60 秒刷新 |

**云端任务实测记录**：2026-09-16 手动触发 `fng-daily` → `completed/success`（run #1）。
日志确认 GitHub **US runner 能正常访问**东财 `datacenter-web`（两融 1600 条）与腾讯日线（1200 个交易日），
并输出"存档无变化：750 天，最新 2026-09-16 = 30.5（恐惧）"—— 幂等守卫生效，未产生噪音提交。

**上线时发现并修复的一个真问题**：控制台原备源顺序是 `API → raw → jsDelivr`，
而 `raw.githubusercontent.com` 在本机网络是"黑洞"（连接既不成功也不拒绝，curl 卡满 20s）。
已改为 `API → jsDelivr → raw` 并给每次抓取加 `AbortController` 超时，
并新增专项测试 `_tests/fng-fallback-test.js`（14 项）覆盖该场景。

**测试总览**：恐贪相关 **202 项**（core 76 + DOM 52 + 备源 14 + UX 60）+ 既有回归 369 项（含预览 7、页面自检 29、逻辑 28、选股 104、攻击 201）= **571 项全绿**。
其中 `_tests/fng-ux-test.js`（60 项）专门覆盖本轮三项优化：吸顶容器与层级、五档建议互不相同/低位必提"不要割肉"/高位必提"不要追高"、曲线滑动定位与纵滑不抢滚动。

**第二轮上线（同日，F20–F22 三项 UX 优化）**：已重新发布，链接不变；线上 `fng-accept.js` 验收 **21 项全绿**
（新增校验：吸顶容器与 `position:sticky`、建议卡、无"观望为主"空话、曲线游标与 `touch-action:pan-y`、历史实证数据已内嵌），线上页面与本地 md5 一致。



### 2.4 高价值功能候选（按"值钱程度"排序，2026-09-16 评估）

**先看清当前的断层在哪**：系统现在是一个**价格穿越提醒器 + 情绪指标查看页**。它只回答两个问题——
"价格到了吗"和"现在多少度"。它**不回答"我该做什么"**，原因有三：

1. 它只认识**价格**，不认识**你的持仓成本** → 所以算不出盈亏，也给不出"该卖多少"。（2026-09-20 主动选择不记账，见下方 V3 行与 2.5）
2. 恐贪指数已经把建议写得很具体（"分批建仓""减仓止盈"），但系统里**没有执行这些建议的工具** →
   **建议和动作之间是断的**，这是最大的一处浪费。
3. 真正该被叫醒的时刻（情绪走到极值、浮亏在扩大、赚到的在回吐）**系统并不知道**，得你自己打开看。

所以"更值钱"的方向不是再加指标，而是**从"提醒器"变成"持仓决策闭环"**。

#### 第一梯队：立刻见效（改动小、直接减少实际损失）

| 编号 | 功能 | 为什么值钱 | 成本 |
|---|---|---|---|
| **V1** | **情绪极值主动推送**：恐贪 < 20 或 > 80 时主动推一条（当前值 + 所处阶段 + 该做什么） | 现在恐贪是"要自己打开的页面"，但**"别割在低点、别追在高点"这件事，靠的就是情绪走到极值那一刻有人提醒你**。把 F21 那张静态建议卡变成在对的时间送到你手机上的消息 | 很小（恐贪云端已每天算好，加一个阈值判断 + 推送） |
| **V2** | **任务心跳 + 失败告警**：每天收盘后自检"该跑的任务跑了吗、推送通道通吗"，异常换通道告警 | 现状是**单点**（GitHub Actions + Server酱）。Actions 因额度/权限静默失败、或 Server酱 欠费，你**完全不会知道** —— 会以为"今天没到价"，其实是"早就没人盯了" | 小 |

#### 第二梯队：体系升级（有依赖顺序，V3 是地基）

| 编号 | 功能 | 为什么值钱 | 依赖 |
|---|---|---|---|
| **V3** | **持仓清单（已 Pivot）**：2026-09-20 明确**只记代码 + 名称，不记成本/股数、不算盈亏**。原计划「成本/股数账本」已放弃 | **关键决策**：放弃记账意味着 V4（按盈亏提醒/移动止盈）、V5（分批计划执行器）失去数据基础，**二者明确移出计划**；系统定位回到「价格穿越提醒器 + 情绪/主力行为研判页」，不做持仓盈亏闭环 | 无 |
| **V4** | **按盈亏提醒 + 移动止盈**：不是"跌到 10.5 叫我"，而是"**这笔浮亏到 −8%**""**这笔浮盈 +25%**""**从最高点回撤 10%**" | 把"价格数字"翻译成"我的钱"。尤其**移动止盈**——A股散户最痛的是"赚了又还回去"，但几乎没人有工具盯这个 | V3 |
| **V5** | **分批计划执行器**：设"跌到 X 买第 1 份、再跌 5% 买第 2 份…共 4 份"，到点提醒该买第几份 | **只有这个系统能做的组合拳**：别处有价格提醒、也有情绪指标，但"情绪到冰点 → 自动进入分批建仓节奏 → 跌一档叫一次"这个闭环没有。直接把 F21 的建议落地成动作 | V3 |

#### 第三梯队：长期王牌（不直接赚钱，但让你越来越准）

| 编号 | 功能 | 为什么值钱 |
|---|---|---|
| **V6** | **提醒历史 + 触发后收益追踪**：每条触发记录带触发价，以及**触发后 5 / 20 / 60 个交易日的实际涨跌** | 你能算出**自己规则的胜率**。三个月后就知道"我设的 −5% 提醒到底有没有用""哪种条件最靠谱"。从凭感觉投资变成有数据可复盘；这份统计本身也是很好的面试素材 |

#### 明确不建议优先做（附理由）

| 想法 | 为什么不值钱 |
|---|---|
| 分时图 / K 线图 | 信息增量低，不解决决策问题；腾讯/东财 App 的看图体验远好于自建 |
| 批量导入股票 | 只省一次输入，边际价值极低 |
| 多用户 / 给别人用 | 架构成本高（见第四节），且你现在没有这个需求 |
| 再加技术指标（MACD / KDJ / 北向…） | 指标越多噪音越多，决策越难；情绪面 F&G 已覆盖 |
| 个股 AI 点评 | 看起来有用但无法验证，容易变成"听着有道理"的噪音 |
| 开盘/收盘定时播报 | 与"心跳"重复度高；且固定时间播报的信息，你打开 App 就有。优先做 V1 这种"事件驱动"的推送 |

#### 建议动手顺序

**V1 → V2 → V3 → V4 → V5 → V6**

- 只想**一周内就感受到价值**：先做 **V1**（情绪极值推送），它直接命中你最初的诉求。
- 想让系统**真正换一个档位**：**V3** 必须做，否则 V4/V5 都无从谈起。
- **V2** 可以随时插队——它平时看不出价值，出问题时是致命的。

### 2.5 落地进度（滚动更新）

| 编号 | 状态 | 落地内容 | 测试 |
|---|---|---|---|
| **V1** | ✅ **已上线**（2026-09-16） | 新增 `push.js`（推送通道单一真源：Server酱 → PushPlus → 企业微信机器人，逐级降级）；`fng-core.js` 增加 `EXTREME / extremeEvent / extremeStreak / isReminderDay`（**边沿触发**：进入/离开极值区各推一次，极值持续每 5 天提醒一次，不会天天轰炸）；`fng.js` 增加 `maybeAlertExtreme`，只在「新增提醒条数 > 0」时推送 | `_tests/fng-extreme-test.js` 58 项 |
| **V2** | ✅ **已上线**（2026-09-16，云端 run #1 success） | 新增 `heartbeat.js` + `.github/workflows/heartbeat.yml`（交易日 23:40 自检：存档是否更新、`monitor.yml`/`fng.yml` 今天有没有跑成功；异常时**换通道**告警并给出排查步骤，周五补一条「一切正常」确认） | `_tests/fng-heartbeat-test.js` 26 项 |
| **V3** | ✅ **已上线**（2026-09-16 初版；**2026-09-20 改为「只记代码+名称、不记账」**） | 新增 `position-core.js`（UMD 单一真源，注入 `console.html`）。**数据模型：持仓与监控是两个独立的列表** —— `config.stocks[]`＝监控清单（只存提醒条件/目标价）、`config.holdings[]`＝持仓清单（**只记 `code/name`**，2026-09-20 起不再存 `cost/qty`）。控制台：独立页 **「我的持仓」**（增 / 删，未监控的持仓一键「开价格提醒」）+ 每只监控卡的**持仓行**（「＋ 记录持仓」入口）。**首页「持仓汇总卡」已于 2026-09-20 随记账口径一并删除**（`renderPortfolio` + `.pf*` 样式整段移除，零调用者，详见 2.5「持仓不再记账」行与二·补6·6） | `_tests/position-core-test.js` 161 项 + `_tests/position-dom-test.js` 627 项（627 = 含 2026-09-20 死代码清除后全量） |
| **★ 透视模式** | ✅ **已上线**（2026-09-17，控制台已发布；云端 `chips.js` 已上传并跑通，首份存档已落库） | 用户原话：「不要干巴巴的走势/风险/机会描述，要从 A 股交易本质出发，分析散户在割肉、追高接筹码、抄底，主力成本、散户成本、主力建仓信号、出货信号、锁仓情况……让我的行为跟主力站一起」。新增 `chip-core.js`（UMD 单一真源，注入 `console.html`）+ 云端 `chips.js`（每日收盘生成 `chips-history.json`）。**同时把原「走势/机会/风险」评测整套下线**（`evalFromBars`/`evalEnsure`/`demoEvalData`/`G.evalCache`/`.hg-eval` 全部移除） | `_tests/chip-test.js` 76 项 + `_tests/position-dom-test.js` 253 项 |
| **信号规则 R2** | ✅ **已上线**（2026-09-17，云端已跑通并首次真实推送） | 用户要求「持仓股连续 2 天及以上都高于 5 日线就提醒卖出」→ 新增第 6 条规则 `MA5_STREAK_EXIT`（**不加乖离条件**，与 HOT_MA5_BIAS 区分开）；新增 `signalsForList()` 把「清单过滤」做成单一真源（**卖出提醒只对持仓股发**）；`buildMessage()` 从 main 里拆出来，推送文案可单测 | `_tests/signal-core-test.js` 113 项 + **`_tests/signals-push-test.js` 33 项**（端到端：合成K线→算信号→过滤→拼文案） |
| **V4** | ❌ **移出计划**（2026-09-20：因放弃持仓记账，无成本/盈亏数据，失去前提） | 按盈亏提醒 + 移动止盈 | — |
| **V5** | ❌ **移出计划**（同上） | 分批建仓计划执行器 | — |
| **V6** | ⏳ 待做（低优先） | 提醒历史 + 触发后收益追踪（不依赖成本，仍可做） | — |
| **主力博弈引擎** | ✅ **已上线**（2026-09-19，控制台已发布） | 持仓详情页「主力博弈」块：三源投票（①当日涨跌 ②当日主力净额 f52 ③20日趋势），`AGREE_RATIO=0.66 / MIN_SOURCES=2`；平票/无多数→降级，趋势持有=连续≥3天冲突且 `ret20>0.15`。冲突天数落 `sa_v3_state_v1`（同日幂等、跨日+1、共识归零、源不足不动计数）。展示层（`v3SrcNote/v3Head/v3Sub/v3Advice`）与算法层（`v3Vote/v3Ensure`）分离；`v3Advice` **不编目标价**。散户行 `retailLineHtml` 纯展示、绝不参与投票；`judgeRetail`：散户净额=−主力净额，必须叠加价格方向 | 见二·补6·1 |
| **S2 中长线关键位置引擎** | ✅ **已上线**（2026-09-19） | `weekStage(code)` 区间级波段定性（底部区间约8日、派发约2~4天），显「第N天」；含 `pos250/premLow/supDist·resDist/wma5·wma20/volRatio/mainRatio/maAlign·streak5·overMa5/mcDev`。四态：`sell-overheat`（高位过热·减仓）/`fall-not-done`（下跌未完）/`accumulate-zone`（主力建仓区）/`trending`（趋势运行中），常量 `ML_LOW_GATE=0.30`…`ML_STOP=0.10`。大盘 `mlMarketCtx()` 只标注不参与触发 | 见二·补6·2 |
| **F4 综合研判** | ✅ **已上线**（2026-09-19；**2026-09-20 降级为中性状态条**） | `fusionVerdict(h)` = 筹码透视（中期）+ 价格结构（短期），纯展示零请求。规则：`force_no_trade`（诱多）硬躲；破位+主力派发→躲；破位+筹码侧为跟→保留跟+`pullback`回踩标注；筹码缺失+破位→观望。**2026-09-20 降级**：常与主力博弈打架，撤下明细格/置信度，改为 `cd-stat mute` 中性状态条（"综合研判（次要参考）→ X（Y）· 以上面主力博弈为准"） | 见二·补6·3 |
| **大盘驾驶舱** | ✅ **已上线**（2026-09-19 云端 `style.js` + 2026-09-20 首页卡） | 云端 `style.js`（S1）每日收盘跑：七档 `defense>theme-run>short-term>rotation>crowd-large>small-cap>mixed` + 操作指南 + 优选≤3只（资金连续≥2日+板块流入前20+`psGate`顶部闸门+冰点退潮不出票+止损−5%）→ `style-history.json`；控制台 `#pgStyle`。首页 `renderDash` 在「今日推荐」**上方**加大盘驾驶舱概览卡（`data-act="dash-style"` → `openStyle()`） | 见二·补6·4 |
| **持仓详情页只留结论** | ✅ **已上线**（2026-09-20 晚） | `v3DetailHtml` 只渲染各引擎**结论**：①主力→X+建议 ②散户行为 ③综合研判（次要参考）中性状态条 ④中长线→X（第N天）+`ws.advice` ⑤缺源warn ⑥免责。已撤下：三源明细格/共识冲突天数/置信度/价格结构评分/S2指标格/波段定性/阶段顶部/恐贪力度/`.cd-note`小字；风控（止损位）与仓位行也已撤（无成本即无基准），但 `mlStopLoss/mlStopHtml/mlPositionHtml` 保留可回退 | `_tests/position-dom-test.js` 627/0 |
| **持仓不再记账 + 死代码清理** | ✅ **已上线**（2026-09-20） | `holdings[]` 只记 `code/name`（2026-09-20 起不再 `cost/qty`，`normHolding` 不再返回 null）；删除首页「持仓汇总卡」`renderPortfolio` + `.pf*` 样式（零调用者，UI 不变）；`position-core.js` 的 `portfolioOf/pnlClass/FEE_NOTE` **保留**给云端与单测 | `_tests/position-dom-test.js` 627/0、`_tests/position-core-test.js` 161/0、`_tests/accept-online.js` **197/0** |

**V3 的三条设计红线**（后续 V4/V5 必须延续）：

1. **（2026-09-20 已作废）原「没记持仓≠持仓为 0」红线**：该红线依赖"未填成本/股数返回 null"，而 2026-09-20 起 `holdings[]` 根本**不再有** `cost/qty` 字段（`normHolding` 直接 `cost:0, qty:0`，记录恒有效）。持仓页**只显示真实价格与结论、不显示任何盈亏数字**，故不再存在"把没记显示成持平"的误导场景。
2. **（2026-09-20 起失效）部分取不到价时总盈亏只按可计价部分算** —— 因已不再计算总盈亏，本条不再适用；行情缺失时改为"只在取到的票上渲染结论"，不编造盈亏。
3. **监控与持仓两份清单互不牵连**：删监控**绝不能**删掉持仓记录；删持仓也**绝不能**动到监控。
   改成本/股数只写 `holdings[]`，绝不写回 `stocks[]`——把成本挂在监控项上，等于强行规定"监控的必须持有"，
   而现实里**持有着但不想被提醒**、**在监控但还没买**都是常态。

> **老数据兼容**：V3 第一版曾把 `cost/qty` 写在 `stocks[]` 上。启动时 `boot()` 会调
> `PositionCore.migrateLegacy(stocks, holdings)` 把它们搬进 `holdings[]` 并摘掉旧字段；
> 该迁移**幂等**（无变化时 `changed=false`，不写盘、不产生只有时间戳变化的噪音提交）。
> 归档记录在恢复监控时，也会把老归档里残留的 `cost/qty` 一并搬进持仓清单。

**注入与构建（本轮起有变化）**：原 `_tests/inject-fng-core.js` 已升级为 **`_tests/inject-core.js`**
（登记表式，一次性注入 `fng-core.js` + `position-core.js`，自带幂等与"内容逐字一致"自检）；
旧的 `inject-fng-core.js` 保留为兼容入口（只注入恐贪核心）。

#### 上线记录（V1–V3，2026-09-16）

| 步骤 | 工具 | 结果 |
|---|---|---|
| 上传云端代码 | `stock-alert-cloud/tools/upload.js` | 8 个文件全部写入仓库，**云端与本地逐字节一致**（`tools/verify-cloud.js` 核验） |
| 验证 workflow 真能跑 | `stock-alert-cloud/tools/dispatch.js` | `heartbeat.yml` run #1 **success 11s**；`fng.yml` run #2 **success 16s** |
| 确认无噪音提交 | 查仓库提交记录 | 恐贪任务跑完**没有**产生 `chore: update fear & greed index` 提交（幂等守卫生效） |
| 发布控制台 | 站点发布 | 链接不变，**线上与本地 md5 一致** |
| 线上端到端验收 | `_tests/accept-online.js` | **32 项全绿**（含"线上注入的持仓核心与本地 `position-core.js` 逐字一致"） |

#### 上线记录（透视模式 + 实时价，2026-09-17）

| 步骤 | 工具 | 结果 |
|---|---|---|
| 本地全量回归 | `_tests/` 14 套 | 全绿：chip 76/0、position-dom 253/0、position-snapshot ALL GREEN、build-preview 17/0、inject-core 4/4、signal-core 90/0、position-core 161/0、console-check 29/0、console-logic 28/0、console-picker 104/0、恐贪五套 + validate 全绿 |
| 发布控制台 | 站点发布（`entryHtml: console.html`） | 链接不变 `https://stock-alert-console.app.workbuddy.host/`，线上与本地 md5 一致 |
| 上传云端 | `tools/upload.js` + `tools/verify-cloud.js` | ✅ **6 上传 / 10 跳过**（新建 `chip-core.js`/`chips.js`，更新 `signal-core.js` R2 / `signals.js` / `review.js` / `signals.yml`；16 个文件**逐字节一致**） |
| 触发验证 | `tools/dispatch.js signals.yml` | ✅ run #2 **success 53s**，8 步全绿；**首份 `chips-history.json` 落库（8 只）**，**首次真实推送已发出（Server酱，3 条信号）** |
| 全量线上验收（带 token） | `_tests/accept-online.js` | **125 通过 / 0 失败（ALL GREEN）** |

#### 上线记录（5 日线卖出推送 + 控制台同步，2026-09-17 第二轮）

| 步骤 | 工具 | 结果 |
|---|---|---|
| 新增规则 R2 + 清单过滤 + 文案拆分 | `signal-core.js` / `signals.js` / `review.js` | 见 2.5 表「信号规则 R2」 |
| 重新注入核心到控制台 | `_tests/inject-core.js` | 4 个核心标记各 1/1、内容逐字一致 |
| 本地全量回归 | `_tests/` 17 套 | 全绿：新增 `signals-push-test` 33/0；`signal-core-test` 90→113；其余（position-dom 253/0、chip 76/0、picker 104/0…）不变 |
| 发布控制台 | 站点发布（`entryHtml: console.html`） | 链接不变，线上与本地 md5 一致 |
| 线上端到端验收 | `_tests/accept-online.js` | **110 通过 / 0 失败**（云端文件判定因匿名 API 限流跳过，需 GH_TOKEN） |

> ⚠️ **验收脚本要能容忍 GitHub 匿名限流**：`api.github.com` 匿名读打多了会返回 **HTTP 403**。
> 403 表示"没读到"，**不等于"文件不在"** —— 只把 404 判为失败，403 打成 ℹ️ 提示（设 `GH_TOKEN` 可解），
> 否则验收会给出假警报。

#### 上线记录（持仓不再记账 + 主力博弈/S2/F4/大盘驾驶舱，2026-09-20）

| 步骤 | 工具 | 结果 |
|---|---|---|
| 死代码影响核验 | `Grep` / `position-dom-test.js` | `renderPortfolio` 全仓仅 1 处（定义，零调用者）；`.pf*` 样式仅被它用；position-dom 627 项前后一致 → 证明删除对结论/UI 零影响 |
| 改负向断言 | `_tests/accept-online.js` | 新写"不得出现"断言一律走 `onNoCmt`（剥离 `/* */`、`//`、`<!-- -->`），避免"注释里写 X 已删除"导致 raw `on.indexOf('X')` 假红 |
| 本地全量回归 | `_tests/`（16 套） | 全绿：position-dom 627/0、position-core 161/0、chip 76/0、signal-core 113/0、build-preview 17/0、accept-online 197/0 等 |
| 发布控制台 | 站点发布（`entryHtml: console.html`，`updateExistingApp:true`） | 链接不变，线上与本地 md5 一致（`898dbe801af0`） |
| 线上端到端验收 | `_tests/accept-online.js` | **197 通过 / 0 失败（ALL GREEN）** |

#### 维护工具（`stock-alert-cloud/tools/`）

| 工具 | 用途 | 关键设计 |
|---|---|---|
| `upload.js` | 上传代码与 workflow 到仓库 | **幂等**：上传前比对云端 md5，内容一致直接跳过（不产生只有时间戳变化的噪音提交）。只传代码，**不碰** `config.json`/`state.json`/`fng-history.json` |
| `verify-cloud.js` | 核验云端与本地是否逐字节一致 | 按 md5 逐文件比对，与 `upload.js` 同一份清单；匿名即可（仓库公开） |
| `dispatch.js` | 手动触发 workflow 并等结果 | 触发 → 轮询 → 打印结论与失败步骤；`--no-wait` 只触发 |

> **上传后的固定动作**：`upload.js` → `verify-cloud.js` → `dispatch.js`（新 workflow 必须 dispatch 一次才算验证过）。
> "HTTP 200 上传成功"不等于"内容对了"，也不等于"云端能跑"。


---

## 二·补 透视模式 · 口径说明（2026-09-17 新增，重要）

> **它回答的问题**：这只票现在**散户在干什么、主力在干什么**，我该站哪边。
> 不是技术面描述，是**从 A 股交易本质出发的行为判断**。

### 补·1 九个判定（用户逐项点名的"九件事"）

| 看谁 | 判定 | 判据（方向 + 资金 + 价格三者的组合） |
|---|---|---|
| 散户 | **割肉** | 价格在跌 + 散户净额为正……反了。正确口径：**价格跌 + 散户净卖出** = 恐慌盘往外倒 |
| 散户 | **追高接筹码** | 价格在涨 + 散户净买入（涨上去才敢买，而主力同时在卖） |
| 散户 | **抄底** | 价格明显下跌 + 散户净买入（越跌越买，往往是接刀） |
| 主力 | **建仓/吸筹** | 主力净流入为正 + 股价还没怎么涨（低位收筹码，最值得跟） |
| 主力 | **拉升** | 主力净流入为正 + 股价已经上涨（推着走） |
| 主力 | **高位派发** | 主力净流出 + 股价还在涨（边拉边卖，把筹码交给接手的） |
| 主力 | **下跌中减仓** | 主力净流出 + 价格跌幅已超过阈值（`ret20 < -8%`） |
| 主力 | **锁仓** | 量能萎缩（换手处自身低位）+ 资金**无方向**（`flowDir(dom20)==='flat'`）。⚠️ 只判缩量会误伤"缩量但主力猛买"，必须叠加资金方向 |
| 主力 | **观望** | 上面都不成立 |

**为什么必须叠加价格方向**：主力净额 = −散户净额（同一笔交易的两面）。只看资金流，
「主力买、散户卖」和「主力卖、散户买」在数值上是镜像，**分不出是割肉还是追高**，
必须要价格方向当第三维。

### 补·2 筹码与成本怎么算（`chip-core.js`）

- **筹码分布**：三角形分布 + 换手率衰减模型，期初筹码铺在第一根 K 线区间内
  （防低换手股把总筹码放大）。参数 `decay=1.0, maxTurnover=0.6, buckets=120`。
- **主力成本** = 现价 ×0.97 **下方**最大的筹码密集峰（吸筹区，也是最强支撑）
- **散户成本** = 现价 ×1.03 **上方**最大的筹码密集峰（套牢/追高区，也是最强压力）
- **获利盘** = 现价以下的筹码占比；**集中度** = 现价 ±10% 区间内的筹码占比
  （≥70% 高度集中 / ≥45% 较集中 / ≥25% 一般 / 否则分散）
- **判定阈值**：主力资金占成交额 ≥4% 算强、≥1.2% 算有方向、低于则视为无方向

> ⚠️ 筹码分布是**按换手率衰减模型估算**的，**不是**真实持仓名册。
> 页面上必须原样标注这句，不能让人误以为是交易所数据。

### 补·3 数据分工（为什么不要两端各算一份）

| 算什么 | 在哪算 | 为什么 |
|---|---|---|
| 筹码分布 + 主力/散户行为 + 站队结论 | **云端** `chips.js`，每天收盘写 `chips-history.json` | 资金流接口（东财 push2his / 新浪 MoneyFlow）在手机浏览器上**不保证能跨域**（新浪无 CORS/JSONP，东财本地被阻断）；且 500 根 K 线 × 120 桶算一次就能给所有设备共用 |
| **5 日线连续站上天数** | **浏览器**（`SignalCore.ma5Streak` + 腾讯日线） | 这个要跟到**今天**，不能等 T+1 |
| 存档里没有这只（比如今天刚加的持仓） | 浏览器**就地实时兜底**（`chipLiveFrom`，东财 JSONP） | 任一接口取不到就降级：股本拿不到跳过筹码分布、资金流拿不到改用"量价近似"，**都能出结论，不会整块空着** |

**取存档也是三源降级**：`api.github.com`（需 token）→ `cdn.jsdelivr.net`（免鉴权、国内可达）
→ `raw.githubusercontent.com`（放末位），**每源都带超时**（API 9s / 备源 8s）。

### 补·4 第 5 日线标识（用户指定口径，与推送信号区分）

| 连续站上天数 | 表现 |
|---|---|
| ≥2 天 | **橙色告警**：「连续 N 天开收盘都站上 5 日线，短线偏热，可先落袋一部分」 |
| 1 天 | 灰色弱化：「刚站上 1 天，继续留意」 |
| 0 天 | **不显示**（不渲染） |

口径：**日线开盘价与收盘价都高于「截止当日」的 5 日线**才算站上，逐日各用当天的 5 日线
（`SignalCore.ma5Streak`），从最新往前数连续天数。**控制台显示与云端推送用的是同一个函数**，
所以「消息里说的天数」和「持仓页显示的天数」永远是同一个数（硬约定：算法单一真源）。

**推送侧（规则 `MA5_STREAK_EXIT`，2026-09-17 加）**：

- 触发条件：`ma5Streak().days >= 2`（参数 `P.ma5ExitDays`），**不加 5 日乖离条件**
- 进哪一栏：`side='sell'` → 当日那条合并消息的 **【卖出/离场提醒】**
- **只对持仓股发**：过滤在 `SignalCore.signalsForList(signals, listKind)` ——
  传 `'monitor'`（只监控、还没买）时把这条滤掉。理由：对一只还没买的票喊"卖出"，
  不但没意义，还会让人以为自己持有。`'holdings'` / `'both'` 都算持仓，照发。
- 防轰炸：靠既有的 `cooldownDays = 5`（同一 code + 同一 rule 5 个交易日内不重复发）。
  这个条件是**持续满足型**，不设冷却会在连涨期间天天喊卖；设了之后最多每 5 个交易日提醒一次。
- 文案：「连续 N 天开盘收盘都站上 5 日线（YYYY-MM-DD 起），短线偏热，按纪律可先落袋一部分
  —— 是减仓提示，不是必须清仓」

> ⚠️ **别和 `HOT_MA5_BIAS` 搞混**：那个要求 5 日乖离 ≥6%，门槛高、不常触发，是"涨太猛"的过热信号；
> `MA5_STREAK_EXIT` 只看天数、门槛低。两条规则**并存、互不替代**。
>
> ⚠️ **这是第一条「以持仓清单为依据」的推送**（此前所有推送都只来自监控清单或全市场级恐贪极值）。
> 持仓股仍**没有**基于成本/盈亏的主动提醒 —— 那是 V4。

### 补·5 配色红线

透视块的配色**刻意避开红/绿** —— 红涨绿跌在本项目里是专表涨跌的，
而「该跟/该躲」是**行为判断**，不是涨跌，混用会让人看错。
故：**跟主力 = 蓝，别接棒 = 橙，留意 = 琥珀，中性 = 灰**。

---

## 二·补3 龙头战法推荐引擎（2026-09-17 重构 screener.js，重要）

用户要求：按龙头战法/游资打法重做每日推荐——研究情绪周期、涨跌停家数、连板高度、题材梯队，
每天给 1~2 只「最有可能连板」的票 + 最佳买入价 + 策略理由（打板/首次分歧低吸/二波），
杜绝推荐进去就接盘，显示推荐后涨跌幅，自己做好回测。**依旧不推送，只写 `picks-history.json`。**

### 补3·1 数据源（都是公开非官方接口，实测口径）

| 源 | 用途 | 关键限制 |
|---|---|---|
| 东财 `push2ex` 涨停/炸板/跌停池 | 情绪判定 + 候选（含连板数 lbc、首封时间 fbt、封单 fund、炸板次数 zbc、行业板块 hybk） | **历史只保留最近 ~15 个交易日**，每天只需要当天+前 6 日，够用 |
| 腾讯 `qt.gtimg.cn` 批量行情 | 昨日涨停股今日溢价（情绪温度计） | 无 |
| 腾讯 `ifzq` 130 根日线 | 候选股结构（一字板/二波/均线） | 本地高并发会弹反爬挑战页（回测改用新浪） |
| 新浪 `quotes.sina.cn` 日线 | **回测专用**（不复权价，涨停判定反而更精确） | 生产端不用 |

### 补3·2 情绪周期五档（阈值集中在 screener.js 顶部 `P.cycle`）

- **输入**：涨停家数 / 跌停家数 / 炸板率 = 炸板/(涨停+炸板) / 空间板高度 / 2板以上家数 / 昨涨停溢价
- **冰点**：涨停 ≤25（或 ≤45 且溢价 <-4%，或空间板 ≤2）→ **不推荐**
- **退潮**：溢价 <-2%（或炸板率 ≥40%，或高度从 ≥5 掉到 ≤3）→ **不推荐**；溢价翻红的「修复日」例外，允许低吸
- **高潮**：涨停 ≥85 且高度 ≥5（或溢价 ≥+5%）→ **只做首次分歧低吸**（高潮追板胜率最差）
- **发酵**：涨停 ≥55 + 炸板率 ≤30% + 2板以上 ≥5 → 打板 + 低吸都开
- **启动**：涨停 ≥35 + 溢价 >0 + 炸板率 ≤35% → 打板 + 低吸都开；其余 = **分歧**，只低吸

### 补3·3 打分（0~100）与防接盘硬过滤（一票否决）

- 打分五维：龙头地位 30 / 梯队完整 15 / 封板质量 20 / 位置结构 20 / 题材强度 15
- 硬过滤（回测与线上同口径）：ST/退、非沪深主板/创业板、流通市值 <20亿 或 >350亿（游资甜区外）、
  换手 >45%、当日炸板 ≥2 次、昨日炸板股、**一字板（买不进）**、20 日涨幅 >100%、
  首封 ≥14:30（尾盘偷袭板）、≥6 板且非全场最高板（鱼尾）
- **买点纪律**（写进每条推荐的 plan，防接盘核心）：
  打板单 = 次日涨停价打板，**高开 >7% 放弃、低开 >3% 放弃**；低吸单 = 次日回踩
  「分歧日低点×1.02 与 5日线×1.01 孰低」，**高开 >3% 不追**、破 10 日线不接；统一止损 -5%。

### 补3·4 存档与版本

- 记录新增字段：`cycle`（周期）、`cycleWhy`（判定依据）、`strat`（策略名）、`buyType`（打板/低吸）、`buyPrice`
- 版本号独立演进：`PICK_VERSION = 'L2'`（不再复用信号 `RULE_VERSION`）；回填胜负口径不变（推荐日收盘起 10 个交易日 ±2%）
- 回测工具：`_tests/leader-backtest.js`——东财池历史只有 15 天，所以用全市场日线**重建**每天的
  涨停/炸板/跌停/连板，喂给 screener 导出的同一套引擎逐日模拟、次日撮合（按买点纪律判定成交与否）。
  用法：`node _tests/leader-backtest.js --days 120`（K线缓存 210 根/股在 `_tests/_cache/`，`--refresh` 强制重建）；
  研究开关：`--studyAll`（结算全部达标候选看条件边际期望）、`--inverse`（只交易被回避的冰点/退潮日，反向验证）、
  `--stopPct/--skipCycle/--minDipScore/--dipCycles/--dipMaxLbc`（参数扫描）。

### 补3·5 L2 校准（2026-09-17 晚，120 日回测定稿）

**方法论**：top-1 口径每天只成交 3-4 笔没法调参（过拟合），先加 `--studyAll` 把全部达标候选
（26 笔）放在一起看「每个条件的边际期望」，再用 top-1 口径验证。反向验证（`--inverse`）先行确认
了冰点/退潮回避是有效负反馈：反向纪律口径 25%/-2.2% vs 正向 50%/+10.2%。

**群体结论（26 笔全部达标低吸，纪律执行口径）**：整体 15%/-1.7% —— **分歧低吸群体本身是负期望**，
+10.2% 全靠「每日只取最高分 1 名」的打分排序 alpha。分层看：
- 启动·低吸 0/4（裸持有 -10.5%）、发酵·低吸 0/6（全部止损）、高潮·低吸 17%/-3.3% → 负期望重灾区
- 连板数：2 板 -0.5%（最优段）、3 板 0/4、4 板 0/3（全部止损）
- 分数：受限群体中 60+ 分 2/2（+25.3%）、50-59 分 1/4（-1.1%）

**L2 三条改动（三口径同向：群体 + top-1 + 战法理论）**：
1. 低吸出手周期收窄为「分歧 / 退潮·修复」（打板仍在启动/发酵）
2. 低吸候选昨日连板 ≤2（`P.guard.dipMaxLbc = 2`）
3. 低吸分数门槛 50→60（`P.dipMinScore = 60`）

**效果（120 日 top-1 口径）**：50%/+10.2%/接盘1 → **67%/+15.2%/接盘0**；受限群体 15%/-1.7% → 50%/+7.7%。
⚠️ 样本仍小（top-1 3 笔、群体 6 笔），方向与理论一致但不是统计显著，继续用每日实盘回填验证；
另：打板口子在 120 日回测中**从未成交**（未封板/高开拦截），其对收益的贡献待实盘观察。

---

## 二·补6 主力博弈 / 中长线 / 综合研判 / 大盘驾驶舱 口径（2026-09-19/20 新增）

> 这几块都是**持仓详情页（`#pgHoldings` 点开单只）与首页**的研判能力，
> 数据来自云端 `chips-history.json` + 浏览器侧腾讯/东财日线，**不引入新推送**（大盘驾驶舱是首页卡 + 独立页，非推送）。

### 补6·1 主力博弈引擎（三源投票）

- **输入三源**：①当日涨跌 ②当日主力净额（东财 `fflow.f52`，可 `null`）③20 日趋势。
- **裁决**：`AGREE_RATIO=0.66`、`MIN_SOURCES=2`；三源里达成多数且一致度≥0.66 才给主信号；平票或无多数 → `downgrade`；**趋势持有** = 连续 ≥3 天冲突且 `ret20>0.15`。
- **状态机**：冲突天数落 `sa_v3_state_v1`（同日幂等、跨日 +1、共识归零、**源不足不动计数**）。
- **分层**：展示层（`v3SrcNote/v3Head/v3Sub/v3Advice`）与算法层（`v3Vote/v3Ensure` + 常量 + 冲突计数）**严格分离**；`v3Advice` **不编目标价**（避免越权给买卖价）。取收盘价用 `v3CloseAt()`（归一化后 `bars[i][2]` 恒失效）。
- **散户不参与投票**：`retailLineHtml(code)` 纯展示（读 `chipMap[code].retail`，零请求）；`judgeRetail`：散户净额 = −主力净额，**必须叠加价格方向**（跌+主力买=割肉 / 跌+主力卖=抄底 / 涨+主力买=获利了结 / 涨+主力卖=追高）。
- **配色**：同透视模式，刻意避开红绿 —— **跟主力=蓝 / 别接棒=橙 / 留意=琥珀 / 中性=灰**。

### 补6·2 S2 中长线关键位置引擎（`weekStage`）

- **定位**：**中长线持股用、不是每天出信号**；区间级波段定性（底部区间约 8 日、派发约 2~4 天），显「第 N 天」；`mlChanged`（`sa_ml_state_v1`）+ `mlRunDays` 数交易日。
- **因子**：`pos250`（用盘中高低价）/ `premLow` / `supDist·resDist`（近 20 周高低=支撑压力）/ `wma5·wma20` / `volRatio`（4 周÷26 周）/ `mainRatio`（近 20 日主力净额÷流通市值）/ `maAlign·streak5·overMa5` / 筹码 `mcDev`（只分级不改触发）。
- **四态**（先判风险后判机会），常量 `ML_LOW_GATE=0.30 / ML_SUP_GATE=0.15 / ML_SELL_MA5=0.05 / ML_SELL_POS=0.65 / ML_SELL_STREAK=2 / ML_STOP=0.10`：
  ① `sell-overheat` 高位过热·减仓（maAlign 且 streak5≥2 且 overMa5>5% 且 pos250>0.65，躲对率 76%）；② `fall-not-done` 下跌未完（ret60<−20% 且 volRatio<0.9 且 |mainRatio|<0.004）；③ `accumulate-zone` 主力建仓区（mainRatio>0.004 且 pos250<0.4 且 wma5>wma20 且 premLow<0.30 且 supDist<0.15，约 0.45 段/年、起点买入 60 日 +24.6%、胜率 90%）；④ `trending` 趋势运行中（兜底）。
- **止损**：`ML_STOP=0.10`：有成本=成本×0.9；无成本=本态起始价×0.9（标「近似」）；都没有=返回 `null`。**仓位 = 用户自控**（五档版已砍）。
- **大盘**：`mlMarketCtx()` 只标注、**不参与触发**（风格七档只有 1 天历史、无法回测）。
- ⚠️ **已验证为错的直觉**（别捡回来）：卖出三直觉全反向（高位+资金转负 +2.96% / 高位+缩量 +5.28% / 高位+周线转空 +5.16%）；「缩量但无资金进场」是陷阱不是底；单独「多头排列+连站 MA5」无预测力（+1.75% vs 基准 +4.45%），**叠加偏离>5% 且 pos>0.65 才有**。

### 补6·3 F4 综合研判（`fusionVerdict`）

- **口径**：筹码透视（中期）+ 价格结构（短期），纯展示零请求。
- **规则**：① `force_no_trade`（诱多）硬躲；② 破位 + 主力派发 → 躲；③ 破位 + 筹码侧为跟 → **保留跟 + `pullback` 回踩标注**（建仓 20 日 +4.76%、黄金组合 +5.50%）；④ 筹码缺失 + 破位 → 观望。
- **回测（17 只×6440 点）**：跟 10 日 +1.34%、躲精准 62.5%（1.54x）、跟后大跌率 25.8% < 基准 28.1%。脚本 `_tests/rules-fusion.js` + `f4-selfcheck.js`。
- **2026-09-20 降级**：F4 常与主力博弈打架（一个说回补吸筹、一个说主力撤退），故撤下三源明细格/共识冲突天数/置信度/价格结构评分/S2 指标格/波段定性/阶段顶部/恐贪力度/`.cd-note` 小字，改为 `cd-stat mute` **中性状态条**（"综合研判（次要参考）→ X（Y）· 以上面主力博弈为准"）。算法层零改动。

### 补6·4 大盘驾驶舱（S1，`style.js`）

- **云端 `style.js` 每日收盘跑**：七档 `defense > theme-run > short-term > rotation > crowd-large > small-cap > mixed` + 操作指南 + 优选 ≤3 只（资金连续 ≥2 日 + 板块流入前 20 + `psGate` 顶部闸门 + 冰点退潮不出票 + 止损 −5%）→ `style-history.json`。
- **控制台**：`#pgStyle` 独立页；首页 `renderDash` 在「今日推荐」**上方**加大盘驾驶舱概览卡（`data-act="dash-style"` → `openStyle()`）。
- **存档读取**：`date` 记**数据所在交易日** + `generatedAt`；控制台取 `days[days.length-1]`（结构 `{v,updated,days:[]}`）。备源 **raw.githubusercontent 先、jsDelivr 后**（CDN 缓存滞后一天）。
- **emoji**：一律用 `\u{1F7E2}` 码点转义正则，绝不按字符串长度切。

### 补6·5 持仓详情页「只留结论」（2026-09-20 晚定稿）

`v3DetailHtml` 只渲染各引擎**结论**，不堆指标：
1. `cd-act`「主力 → X」+ 建议；2. `retailLineHtml`（散户行为）；3. `cd-stat mute`「综合研判（次要参考）→ X（Y）· 以上面主力博弈为准」；4. `cd-act`「中长线 → X（第 N 天）」+ `ws.advice`；5. 缺源 warn；6. 一行免责。
**已撤下**：三源明细格、共识/冲突天数/置信度、价格结构评分、S2 全部指标格、波段定性说明、阶段顶部、恐贪力度、`.cd-note` 小字；**风控（止损位）与仓位行也已撤**（无成本即无基准），但 `mlStopLoss/mlStopHtml/mlPositionHtml` 保留可回退。
`ws.advice` 里的代价/口径数字（0.45 次、再跌 2.9%、2025 反向、躲对率 76%）必须说清，别顺手删；强调文案走 `mdB(esc(...))`。

### 补6·6 持仓不再记账 + 死代码清理（2026-09-20）

- **决策**：`holdings[]` 只记 `code/name`，**删除成本/股数**（`normHolding` 不再返回 null，直接 `cost:0, qty:0`）。理由：空窗期 + 求职期不值得维护账本；系统定位回到"提醒器 + 研判页"。
- **删除**：首页「持仓汇总卡」`renderPortfolio(map)` 整函数 + 全部 `.pf*` 样式（零调用者，UI 不变，position-dom 627 项前后一致）。`position-core.js` 的 `portfolioOf / pnlClass / FEE_NOTE` **保留**（云端与单测仍在用）。
- **测试硬约束**：改「不得出现 X」类断言必须走 `onNoCmt`（剥离 `/* */`、`//`、`<!-- -->`）；**删代码时最容易翻车** —— 一旦在注释里写「X 已删除」，任何 raw `on.indexOf('X')` 负断言立刻假红，新写负断言一律用 `onNoCmt`。

---

## 三、关键约束（换任何方案都绕不开的）

| 约束 | 内容 | 影响 |
|---|---|---|
| **推送必须由服务端发起** | 微信不允许客户端自己给自己发消息 | 必须有"一台 24 小时在线的机器"（云端） |
| **免费推送额度** | Server酱 免费版约 5 条/天 | 超了要换 PushPlus（额度更宽）或企业微信机器人（无限） |
| **行情数据源** | 东方财富、腾讯的**公开非官方接口** | 有几十秒~几分钟延迟；**不能作为交易依据**；接口将来可能失效（代码已内置多域名容灾） |
| **检查频率下限** | GitHub Actions 定时任务最小 5 分钟 | 且整点附近可能延迟 1~3 分钟（服务器排队） |
| **前复权说明** | 当前成交价在复权/不复权下**相同**，所以实时比价直接用最新成交价 | 这是正确做法，不是简化 |
| **合规红线** | 小程序 iOS 端禁止虚拟商品支付；禁止诱导分享；类目资质需匹配 | 涉及时必须查微信官方最新规则 |

---

## 四、未来形态评估：网页 / 小程序 / APP

### 形态一：网页控制台（✅ 现在就是）

- 成本 **0 元**，无需审核，改完即生效
- 手机上在微信里打开即可，可收藏
- **局限**：不能在微信里被搜索到、不能分享给别人用、没有原生小程序的体验

### 形态二：微信小程序（代码已写完，未部署）

> **重要**：小程序版的完整代码**已经存在**，在 `D:\mywork\stock-monitor\`，共 31 个文件。
> 包含 4 个页面（监控列表 / 添加股票 / 详情含前复权 K 线图 / 使用说明）
> 和 2 个云函数（`stockApi` 行情与搜索、`monitor` 定时检查 + 订阅消息推送）。
> **不是从零开始，只差部署。**

- 需要：开通**云开发**（付费套餐，约 19.9 元/月起）+ 微信**审核**
- 优势：原生体验最好；可上架给不特定人群使用
- 特别注意：订阅消息是"授权一次发一条"，需要用户自己攒额度（个人主体申请不到长期订阅）
- 部署前置条件（只有你能做）：开通云开发环境、在公众平台配置订阅消息模板

### 形态三：手机 APP（需要重写）

- 小程序代码**不能**直接转成 APP（不同技术栈）
- 但**业务逻辑 100% 可复用**：`alert.js` 的行情解析、条件判断、冷却去重逻辑可以整体搬过去
- 技术选型建议：
  - **uni-app / Taro**：一套代码同时出小程序 + APP（推荐，最省力）
  - **Flutter**：性能好，但要重写界面
  - 原生 Android/iOS：成本最高，不推荐
- 额外成本：开发者账号（iOS 需 688 元/年）、应用商店审核、推送通道（极光/个推等）

### 对比总表

| | 网页控制台 | 小程序 | APP |
|---|---|---|---|
| 现状 | ✅ 已上线 | 代码已完成，未部署 | 未开始 |
| 开发成本 | 已完成 | 低（部署 + 微调） | 高（重写界面） |
| 运行成本 | 0 元 | 云开发约 19.9/月起 | 服务器 + 推送服务 |
| 审核 | 不需要 | 微信审核 | 应用商店审核 |
| 通知方式 | 公众号推送（额度限制） | 小程序订阅消息（需攒额度） | APP 推送（无限制） |
| 适合场景 | 自己用 | 小范围正式使用 | 面向公众 |

---

## 五、只有你有的信息（任何代码和文档都替代不了）

| 信息 | 当前状态 | 用到的时候 |
|---|---|---|
| 小程序 AppID | ✅ `wx334116fdd8e07845` | 小程序部署 |
| 云开发环境 ID | ❌ 未开通 | 小程序部署 |
| 订阅消息模板 ID + 4 个关键词编号 | ❌ 未配置 | 小程序通知 |
| Server酱 SendKey | ✅ 已配好（存在 GitHub Secret `SENDKEY`） | 云端通知 |
| GitHub 用户名 / 仓库名 | ✅ `zyhaaad` / `stock-alert` | 云端监控、控制台 |
| GitHub Token | ⚠️ 在控制台 localStorage 里，**易丢失**；另有一个用于上传云端文件的一次性 token，**上线后建议立即到 GitHub 吊销**（Settings → Developer settings → Personal access tokens） | 控制台读写配置 / 云端文件上传 |

---

## 六、项目文件地图

| 目录 | 是什么 | 状态 |
|---|---|---|
| `stock-alert-cloud/` | **云端监控本体**（GitHub 仓库的那份）。算法核心（UMD 单一真源）：`fng-core.js`（恐贪）、`position-core.js`（持仓清单）、`signal-core.js`（信号 + `ma5Streak`）、`chip-core.js`（透视）。任务脚本：`fng.js` / `heartbeat.js` / `alert.js` / `signals.js` / `screener.js` / `review.js` / `chips.js` / **`style.js`（大盘驾驶舱 S1，每日收盘跑）**。存档：`fng-history.json` / `signals-history.json` / `picks-history.json` / `chips-history.json` / **`style-history.json`** | **线上运行中（2026-09-20 全量上线，含大盘驾驶舱）** |
| `stock-alert-console/` | **手机网页控制台**（单文件 `console.html`：首页 `#dash`（含大盘驾驶舱卡 + 今日推荐）、恐贪页 `#pgFng`、持仓页 `#pgHoldings`（只留结论）、推荐页 `#pgPicks`、大盘驾驶舱页 `#pgStyle`；引擎 `v3Vote/v3Ensure`（主力博弈）、`weekStage`（S2 中长线）、`fusionVerdict`（F4 综合研判）均内置） | 线上运行中（2026-09-20 发布，md5 `898dbe801af0`） |
| `stock-alert/` | 本地版（Windows 任务计划，已被云端版替代） | 备用 |
| `stock-monitor/` | **小程序版完整代码** | 未部署 |
| `_tests/` | 测试与构建脚本。**核心注入**：`inject-core.js`（登记表式，一次注入 fng/position/signal/chip 四个核心，自带幂等与"逐字一致"自检）；**单测**：`fng-*-test.js`、`position-core-test.js`、`signal-core-test.js`、`chip-test.js`；**链路测试**：`signals-push-test.js`（合成K线→算信号→按清单过滤→拼推送文案，不联网）；**DOM 测试**：`fng-dom-test.js`、`position-dom-test.js`；**快照/构建**：`fng-snapshot.js`、`position-snapshot.js`、`build-preview.js`、`console-check.js`、`console-logic.js`、`console-picker.js`；**线上验收**：`accept-online.js` | — |
| `股票监控系统-需求与方案说明.md` | 本文件 | — |

> ⚠️ **跑测试的顺序有讲究**：`build-preview.js` 会生成 `_preview/stock-picker-preview.html`，
> 而 `position-dom-test.js` 读的就是这份产物 —— **必须先 `inject-core.js` → `build-preview.js` → 再跑 DOM 测试**，
> 否则测的是上一轮的文件（会假绿）。`console-check.js` 会顺手重写 `design-preview.html`，别手改。

---

## 七、要加功能时的操作口令

对 AI 说下面任意一句就能开工，**不需要重讲需求**：

- 「读一下 `D:\mywork\股票监控系统-需求与方案说明.md`，我要加 XX 功能」
- 「把小程序版（`stock-monitor`）部署上去」
- 「云端监控出问题了，帮我查」
- 「按 2.4 节的 **V1** 开始做」——直接照价值排序表点编号，不用解释背景

---

## 八、换机 / 交接清单（如何在新电脑上接手）

> 本文档是「需求单一真源」，但**代码与记忆有两处不在文档里**，必须一起带走才能无缝接手。

### 8.1 资产分布与去哪拿

| 资产 | 位置 | 新机怎么拿 |
|---|---|---|
| 云端脚本（fng/position/signal/chip/style 核心 + 任务） | GitHub `zyhaaad/stock-alert`（远程才是真源） | `git clone` 或下载；本地 `stock-alert-cloud/` 只是镜像 |
| 控制台 `console.html` + 全部测试 `_tests/` + 本文档 | GitHub `zyhaaad/stock-alert-console`（**已建，克隆即取**，77 文件 + README） | `git clone https://github.com/zyhaaad/stock-alert-console.git` |
| 部署后的控制台 | `https://stock-alert-console.app.workbuddy.host/` | 仅运行产物兜底，不是编辑源 |
| 项目记忆 `.workbuddy/memory/` | 本机独有 | 需一并拷贝（见 8.4） |

> ✅ 本机已执行：`D:\mywork` 已 `git init`，`.gitignore` 为**白名单**——默认忽略一切，**只收纳** `stock-alert-console/`、`_tests/`、`股票监控系统-需求与方案说明.md`；本机个人文件（`_private/`、`简历优化-张耀华/`、`stock-alert-cloud/` 镜像等）全部被忽略，不会误进版本库。初始提交 `b0cbcab`。

### 8.2 把仓库带到新机（三选一）

- **方式 A｜整目录复制（最简单）**：把整个 `D:\mywork`（含隐藏的 `.git`）拷到新机同路径，直接 `git` 即可恢复历史。
- **方式 B｜推到 GitHub 再 clone（已落地，最稳）**：仓库 `zyhaaad/stock-alert-console` 已建并推好（main 三提交：种 README `55485249` → 77 文件 `31e4dd60` → 补回 README `2526fa94`），新机直接：
  ```bash
  git clone https://github.com/zyhaaad/stock-alert-console.git
  ```
  ⚠️ 本沙盒 `git push` 走 github.com 被代理 502 拦截，故本地推送走 **GitHub Git Data API**（脚本 `_tests/_cache/push-github-v2.js`，token 经 env `GH_TOKEN_PUSH` 传入、不落盘、不拼进 remote URL）；日常再同步同理，**切勿把 token 写进 `.git/config` 或 remote URL**。另：`zyhaaad/stock-alert` 是云端脚本仓，与本仓相互独立，不要混。
- **方式 C｜同步盘**：用 OneDrive / 微云等同步 `D:\mywork`，自动跨机。

### 8.3 新机接手步骤

1. **取代码**（两仓独立）：
   ```bash
   git clone https://github.com/zyhaaad/stock-alert-console.git   # 控制台 + 测试 + 需求文档（<ROOT>）
   git clone https://github.com/zyhaaad/stock-alert              # 云端脚本（zyhaaad/stock-alert 才是真源，本地 stock-alert-cloud/ 只是镜像）
   ```
   保持 `stock-alert-console/` 与 `_tests/` 同层（即 <ROOT> 下）。
2. **配环境（只跑测试用）**：Node 22 + Python 3.13（用 WorkBuddy 托管运行时即可，无需全局安装）。
3. **读文档**：第六节「文件地图」看谁在哪、第七节「操作口令」直接开工。
4. **改代码前先跑回归基线，确认全绿再动**（顺序见第六节末尾）：
   ```bash
   node _tests/inject-core.js
   node _tests/build-preview.js
   node _tests/position-dom-test.js     # 627/0
   node _tests/position-core-test.js    # 161/0
   # 其余见 2.3 / 2.5 的回归记录（共 16 套）
   ```
5. **凭证**：`SENDKEY`（Server酱）是 GitHub Secret，常驻不用重配；上传云端用的 GitHub Token **每次发布后吊销**，新机需重新生成 classic token（repo+workflow）。

### 8.4 记忆文件（踩坑史）要一起带

`.workbuddy/memory/MEMORY.md` + 近期 `YYYY-MM-DD.md` 记录了大量**文档之外的硬约定与踩坑**（负断言必须 `onNoCmt`、发布同意不跨轮、`renderPortfolio` 死代码、S2 口径细节等）。它们**不随本文档走**，换机时请一并拷贝到新机的 `D:\mywork\.workbuddy\memory\`；若不带，则接手 AI 只能依赖本文档，部分历史坑需重新踩。

### 8.5 给新机器 AI 的开场白（直接复制）

> 你是接手「A股价格监控系统」的 AI 助手。先取代码：
> ```bash
> git clone https://github.com/zyhaaad/stock-alert-console.git   # 控制台 console.html + 全部测试 _tests/ + 需求文档（克隆到任意目录，下文用 <ROOT> 代指）
> git clone https://github.com/zyhaaad/stock-alert              # 云端脚本（zyhaaad/stock-alert 才是真源，本地 stock-alert-cloud/ 只是镜像）
> ```
> 接手顺序：① 读 `<ROOT>/股票监控系统-需求与方案说明.md`（需求与方案单一真源：文件地图、落地进度、各引擎口径、第八节换机清单）；② 读 `<ROOT>/.workbuddy/memory/MEMORY.md` 与当日 `YYYY-MM-DD.md` 了解**文档之外的踩坑史**。**若 `.workbuddy/memory/` 不存在，说明记忆没带过来，请直接告诉我「记忆缺失」，不要假装知道历史坑、也不要重新踩一遍。** ③ 改动前先跑回归（顺序：`inject-core.js` → `build-preview.js` → `position-dom-test.js`，详见文档第六节末尾），全绿再动手。发布走 `workbuddy_sites_deploy`（`entryHtml: console.html`、`updateExistingApp: true`），且**必须等我在本轮明确说"发布"**才可同步线上。改模型 = position-core + console.html + build-preview 注入 + 测试期望，缺一不可；任何对代码/模型的改动都要同步更新本需求文档。
