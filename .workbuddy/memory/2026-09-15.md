# 2026-09-15 工作日志

## 简历优化（张耀华）
- 读取并解析 `C:\Users\29086\Downloads\张耀华-产品经理 (2).pdf`（用 pdfplumber 提取文本，pypdf 已装在 managed Python）
- 输出：`D:\mywork\简历优化-张耀华\张耀华-评分报告.md`（71/B-，五维度评分 + Before→After + 40+ 清单）
- 输出：`D:\mywork\简历优化-张耀华\张耀华-资深产品经理-优化版.md`
- 关键改动：重定位为"资深产品经理/产品负责人"；重写博彩类敏感表述（概率玩法→游戏化互动玩法，合规作为卖点）；删除工作经历与项目经历重复（相知语音）；删高中教育；技能区扩充
- 注意点：用户简历中 2026.01 离职后有约 8 个月空窗；抓马直播 DAU 20 万等数据口径需面试前自查；简历主格式为 Markdown，待用户提供 JD 可出定制版

## A股监控：新增「删除记录 + 监控天数」（云端版功能扩展）

**用户需求**：删除的股票要归档——提醒条件只读、可看「达到条件触发后的价格涨跌幅」、已删除天数；未触发的不显示；正在监控的股票显示已监控天数；两区分开、页面体验要好。

**顺带修掉一个真 bug**：workflow 的保存状态步骤写的是 `! git diff --quiet state.json`，而 state.json 每次都是**新建的未跟踪文件**，git diff 检测不到 → 状态从未提交回仓库 → 运行期每次判为"首次满足"，**条件成立时每 5 分钟重复推送**。改为先 `git add` 再 `git diff --cached --quiet`，实测已生效（仓库里已出现 state.json）。

**实现**：
- `alert.js`：条件满足时写 `firstFire{price,at}`（只记一次，作为涨跌幅基准）+ `lastFire` + 真实推送次数 `fireCount`；新增 `--simulate`（忽略交易时段、不推送、仍写状态，用于安全验证）；test 模式不再写状态（避免污染存档）
- `config.json`：`stocks[]` 加 `addedAt`（已监控天数），新增 `deleted[]`（`condition/target/addedAt/removedAt/fire` 快照）
- `console.html`：两区渲染（正在监控 / 删除记录），删除=归档（从 state.json 快照触发价），支持恢复监控与彻底移除；未触发记录默认隐藏可展开；条件在存档区只读
- workflow 加 `workflow_dispatch` 布尔输入 `simulate`，随时可安全跑测试

**验证**：本地桩测试 13 项（价格变化 → firstFire 只记一次、lastFire 更新、--dry 不写状态等）全通过；控制台静态校验 19 项全通过；云端 dispatch simulate 运行 success 且 state.json 成功回写仓库。

**部署**：用 token（repo+workflow）经 Contents API + Git Data API 上传 alert.js / config.json / workflow。GitHub classic token **缺 `workflow` scope 时写 workflow 文件返回 404**。

**待办**：用户仓库里两只股票（海康威视 lte 31、北汽蓝谷 lte 3）的 `addedAt` 是迁移时按 2026-09-15 补的，天数可能与实际有 1 天误差。

（16:20 更新）新版控制台**已发布上线**，链接不变：https://stock-alert-console.app.workbuddy.host/ 。发布时明确告知用户：该能力只能发布**网页**，不能构建/签名/上线小程序；网页项目用微信开发者工具打开也无法变成小程序。

## A股监控：需求固化为文档（应对"以后要做 APP / 小程序"）

**用户问**：以后要把功能做成 APP 或小程序，还需要重新讲需求吗？

**答复要点**：
1. 不用重讲——需求沉淀在三处：代码本身（小程序版 31 个文件在 `stock-monitor/`）、我的项目记忆、以及技能库
2. 但记忆是本机私有、会被清理的 → 所以把需求**固化进项目文件**
3. 产出：`D:\mywork\股票监控系统-需求与方案说明.md`（F1~F13 已实现清单 + 候选功能池 + 6 项硬约束 + 三种形态评估 + 只有用户才有的信息清单 + 项目文件地图 + 加功能口令）

**关键事实（别丢）**：
- 小程序版代码**已完成未部署**（`stock-monitor/`：4 页面 + stockApi/monitor 两个云函数），不是从零开始
- AppID `wx334116fdd8e07845` 已有；云开发环境、订阅消息模板 ID 都没配
- APP 形态：小程序代码不能直接转换，但 `alert.js` 的业务逻辑（行情解析/条件判断/去重冷却）100% 可复用；选型建议 uni-app / Taro
- 只有用户本人有的信息：云开发环境 ID、订阅消息模板 ID 与关键词编号（其它都已就绪）

## A股监控：控制台布局重设计 + Token 三层保管（同日 16:30）

**用户要求**：① Token 要更稳妥的保存方式；② 页面原型布局优化——删除记录 / 价格刷新 / 添加股票都不是高频操作，内容展示别杂乱。

**设计原则（按操作频率分层）**：高频「看」留首屏，低频「改」全部收起。
- 主区只放卡片（名称+代码 / 现价涨跌 / 条件 + 已监控天数 / **离触发还差多少**），操作按钮从卡面消失
- 点卡片任意位置或右上 ⋯ 展开操作区（改条件 + 删除）
- 添加股票 → 右下 FAB → 全屏表单页（含实时规则预览）
- 删除记录 / 设置 → 右上 ⋯ → bottom sheet → 全屏子页
- 归档区分「已触发 / 未触发」两个 tab，替代原来的「点此查看/收起」链接
- 价格刷新改为**静默自动**（进页面 + 回到前台 + 每 30s 走时钟），顶部小字显示"刚刚更新/3 分钟前"，点击可手动刷新

**配色语义修正（重要）**：A股语境下红色 = 涨，原版把红色 `#c62828` 同时用作主按钮色，语义冲突。新版主操作色改中性深色 `#23262b`，**红色只留给"涨"**（`#e23a3a` / 跌 `#0aa457`）。

**抓到的真 bug（已修 + 已加回归断言）**：`gapInfo()` 里 `lt` 用了 `price <= target`、`gt` 用了 `price >= target`，没和 `lte`/`gte` 区分。后果：设「价格 < 31.00」且现价正好 31.00 时，界面显示"已达条件"但云端严格 `<` 不触发 → 界面与实际不一致。已改为按 cond 精确分支；并核对云端 `alert.js` 的 `checkCond`（`gt`=严格`>`）确认两端一致。

**Token 三层保管方案**（产出 `D:\mywork\Token保管方案.md`）：
1. 换 **fine-grained token**（只授权 stock-alert 单仓库，Contents + Workflows 读写）——把风险从"全账户"降到"单仓库"
2. 控制台「生成备份」→ AES-GCM（PBKDF2 派生，随机 6 位数字口令）→ 复制整段存**微信收藏**（云同步，换手机也在）；打开带 `#k=` 的链接自动提示恢复
3. 本机兜底：`D:\mywork\_private\凭据备份.txt`（Token 用 Base64 混淆）

**⚠️ 位置红线**：凭据文件**绝不能放 `stock-alert-console/`**，因为发布上线的就是这个目录，等于把钥匙挂到网上。已放 `_private/`。

**回归测试（本地私有，未随站点发布）**：`D:\mywork\_tests\console-check.js`（29 项：脚本语法、微信兼容红线不用 `?.`/`??`/`Intl` 时区、DOM id 完整性、标签配对、关键文案）+ `console-logic.js`（28 项：secidOf 归属、五种条件的差值真值、异常输入不崩、涨跌配色）。改 console.html 后务必重跑这两个。

**待用户确认**：新版尚未发布上线（`console.html` 已改好，`design-preview.html` 为带示例数据的预览版）；用户确认布局后再覆盖线上链接。

## A股监控：添加股票支持名称/拼音（同日 17:30，已上线）

**用户需求**：股票添加方式支持名称添加；不存在或同名称多个代码时给出选择操作。

**走的流程**：设计原型专家团（design-engine-stockpicker）五阶段 SOP —— 需求发现 → 设计令牌 → 原型实现 → 独立质量审查 → 交付。产出文档留在 `D:\mywork\_design\`（phase1-requirements / phase2-design-tokens / phase4-review / recon-api-facts）。

**接口事实（实测，别再重新踩坑）——详见 `_design/recon-api-facts.md`**
- 搜索接口 `searchapi.eastmoney.com/api/suggest/get?input=&type=14&token=D43BF722C8E33BDC906FB84D85E326E8&count=`，支持 JSONP，**count 上限至少 50**（不是 12）
- **品类混杂**：同一关键词返回 A股/债券/基金/港股/美股/日股/三板/板块/期货/期权
- **A 股判定不能用 `Classify==='AStock'`**：北交所（贝特瑞 920185）类型名是「京A」但 `Classify==='NEEQ'`，只判 AStock 会**漏掉整个北交所**
- **也不能按 `MktNum` 0/1 筛**：B股（深物业B 200011）`Classify==='BStock'` 而 `MktNum` 与深市 A 股同为 `0`，会误收；债券同样混入
- 正确判定：`SecurityTypeName ∈ {沪A,深A,京A}` 且排除 `BStock`；兜底 `Type为空 && Classify==='AStock'`
- **同名多代码是真实且常见的**：`zsyh` → 招商银行 AND 浙商银行（简拼歧义）；"深物业" → 深物业A(000011) + 深物业B(200011)（A/B 同名）；"平安银行" → 1 只深A + 4 只同名债券
- **名称污染**：`600016` 返回的名称就是 `XD民生银`（除权前缀）；另有 ST/*ST/退市 前缀 → 比对前须归一化，展示仍用原名
- 拼音简拼原生可用（gzmt→贵州茅台）但**有歧义**（zsyh 命中两家银行），不能自动选定

**实现要点**（`stock-alert-console/console.html`，995→1448 行）
- 单一搜索框（名称/全拼/简拼/6位代码自动分流）→ 结果区四态（唯一已选中卡 / 候选列表 / 空态 / 非A股品类提示）→ 条件与目标价
- 点击触发搜索（不做防抖），加载骨架**必须在 await 之前同步渲染**（弱网反馈）
- 唯一命中**自动选中但不自动提交**；同名多代码**绝不自动选**；非 A 股（如"腾讯控股"只有港股）必须明确说"不在 A 股"而非"没搜到"，且**不提供"仍然添加"入口**
- 降级通道保留：搜索失败 → 自动展开「手输 6 位代码 + 手填名称」并把焦点跳到代码框
- 纯逻辑抽成 `SR-LOGIC-BEGIN/END` 标记块（srIsA/srRank/srBranch/srSuggest/srNormQuery…），**不依赖 DOM/网络**，便于脱离浏览器测试

**审查抓到的两个会「默默加错股票」的 bug（已修）**
1. unique 分支命中「已在监控中」时直接 return，**没清掉上一次的选中字段** → 用户接着填价格提交，加进去的是上一只
2. `srReset`（打开添加页时）只清了搜索框和结果区，**没清 #fCode/#fName** → 每次打开都带着上次选中

**其它修复**：建议词由「剥前缀的后缀」改为**完整候选名**（旧行为点"平安"反而越搜越宽）；补齐"选中后自动带出现价作目标价参考占位"（异步 + 请求序号 + 比对当前 code 防竞态，失败静默）；全角→半角归一；`.sr-chip` 配色提升到 AA；`.sr-nm` 加省略号。

**验证**：`_tests/console-picker.js` 104 项 + `_tests/phase4-attack.js` 200 项，全绿 0 警告；独立审查 5 维 22/25（哲学4/层次4/执行5/特异性4/克制5），Anti-Slop 无 P0（新增 33 行 CSS 零新色、无第三字重、无微信禁用项）。

**已上线**：https://stock-alert-console.app.workbuddy.host/ （链接不变）；`design-preview.html` 由 `_tests/make-preview.js` 从 console.html 注入 `window.__FORCE_DEMO__=true` 生成，**改完 console.html 要重新生成**。

**演示模式关键词对照**（用 `?demo=1` 或预览页体验）：贵州茅台/gzmt → 已在监控中；招商银行 → 唯一命中（happy path）；深物业 → 同名多代码；平安 → 泛关键词（两条都属已归档，会提示可重加）；中国 → 泛关键词 + 截断 + 建议词；腾讯控股 → 非 A 股品类提示；不存在zzz → 空态。

**遗留**：`srNormName` 未剥 ST/*ST（可接受，查重以 code 为准）；未真机实测微信 X5（静态证据充分）；demo 里 `000014` 被命名为「深物业A」是**人造数据**（实为沙河股份），仅演示用。

## 游戏外挂需求 → 拒绝并转向合规学习路线（23:10）

**用户原始需求**：编写传世（传奇世界）全自动辅助脚本需求文档——自动打怪/捡物/回城买药/任务、遇玩家自动攻击或飞走、激活码按时长授权售卖。

**处理**：明确拒绝（制售网游外挂涉刑法 285/217，激活码收费模式正是司法重点打击形态），提供替代方向；用户选择"研究脚本用于学习"。

**产出**：`D:\mywork\游戏脚本自动化-合规学习指南.md` —— 三概念分界（Mod/官方脚本/外挂）、L1-L5 技术分层风险表、法条红线速览、五条合规学习路线（单机 Mod、官方 Bot API 如 PySC2/OpenTTD、自研游戏+自写自动化、开源游戏 CV 自动化、游戏安全就业方向）、原需求功能→正规技术映射表、5 级动手项目阶梯、红线清单。

**后续抓手**：用户可能要做项目 1（Python 命令行挂机 RPG）或激活码签名系统——两者都合法，可正常推进。

## 上线状态核验（17:45，回应"还有多少问题，赶紧上线"）

**结论：无阻断项，线上已是最新版。**
- Phase 4 审查的 2 项「重要」+ 5 项「建议」**全部已修**并回归：`srIsA` 兜底（A1=true）、建议词回填全名（B4 返回 `中国平安/中国人寿/中国石油`）、`srFillPriceHint` 目标价占位（D7）、`srFindItem` 加 `srIsA` 过滤（1061 行）、`.sr-chip` 改 `#5f6368`（建议-3）、`.sr-nm` 加省略号（143 行）、全角归一（`srNormQuery`）
- 测试全绿：`console-picker` 104 / `phase4-attack` 201（0 警告）/ `console-check` 29 / `console-logic` 28 / `build-preview` 7
- **线上与本地一致性核验**：`GET /console.html` 返回 70255 B，与本地 `console.html` 字节数一致，且新标记（`srFillPriceHint`/`srNormQuery`/`SR-LOGIC-BEGIN`）线上全部存在 → 线上就是最新版（哈希不同仅因 PS 按 Latin-1 解码响应，非内容差异）
- 站内结构：`/` 与 `/index.html` 均 186 B 跳转页 → `./console.html`，所以打开根链接即可，**链接不变**：https://stock-alert-console.app.workbuddy.host/
- 环境注意：本轮 Bash 工具不可用（`dirname: command not found`）、PowerShell 直接输出不返回 → **变通法：PS 把结果写 `$env:TEMP\*.txt`，再用 Read 工具读**；Glob 对 `D:/mywork/...` 绝对路径返回空，用 PS 列举替代

（17:47 补充）用户要求"立即上线"→ 用 `workbuddy_sites_deploy`（directory=stock-alert-console, language=static, updateExistingApp=true, domainPrefix=stock-alert-console）**重新发布成功**，`verified:true`，**shareLink 不变**：https://stock-alert-console.app.workbuddy.host/ 。发布后复测：HTTP 200 / 70255 B，新标记 `srFillPriceHint`/`srNormQuery`/`SR-LOGIC-BEGIN`/`sr-chip` 全部在线。该目录**只有 console.html 一个文件**，无 index.html（根路径的 186 B 跳转页由托管层生成）。


## 信号引擎落地：screener 数据源迁移 + 控制台三页分离（17:00-18:00 续）

**screener.js 数据源迁移（东财限流 → 腾讯）**：
- 东财 clist 本地 IP 被限流（socket hang up），换 `proxy.finance.qq.com/cgi/cgi-bin/rank/hs/getBoardRankList`
- **接口口径（探针实测，别再猜）**：合法 sort_type 只有 `price/turnover/volume`（zdf/zdf_d60 等报 error_type）；单页 count ≤ 200；深翻页到 total 都正常（4602 条全拉通）；`board_code=aStock` 天然不含科创板/北交所（sh68 前缀 0 条，total≈4600）；**turnover 单位=万元**（茅台 33 亿 ≈ 330793）；zdf_d60=60日涨幅百分比数；state='S'=停牌
- 缺行业/上市日期字段 → 行业用东财 ulist 按 secids 分批（50/批）补齐，失败降级「未知」；次新靠段2「K线<60根自然淘汰」
- 冒烟全通：4602 → 过滤 3107 → 前250 → 达标180 → 备选2只（贤丰控股/津药药业 11分）
- **补上胜负回填缺口**：screener 原来只写不判 → 加「段0」用 `S.judgeOutcome({side:'buy',at,price}, bars)` 回填 verdict（`closeAfter` 要求存档里能找到信号日，停牌对不上会 nodata）

**console.html 三页分离（用户明确规格：监控不要持仓操作、三清单独立展示）**：
- 删：个股卡持仓行 posLineHtml、面板 posgroup/e-cost/e-qty、posEdit 动作、添加页「顺手记持仓」、renderPortfolio 在首页的渲染与 btnGoHoldings、.pos/.posgroup CSS
- 增：pgPicks 页（读云端 picks-history.json，复用 fngFetch/authHeaders 三源降级）、rowPicks 入口、picksDemoData、pickVerdictHtml（A股红涨绿跌语义）
- 移：renderPortfolio 渲染进 pgHoldings 的 #pfBox
- SIGNAL-CORE 注入（inject-core.js REG 加 signal 项，标记 3/3 逐字一致）
- **教训重演**：改完 console.html 忘了重跑 build-preview 就跑 position-dom-test → 182 全绿是假的（测的旧预览副本）。**必须先 build-preview 再跑 DOM 测试**。position-dom-test 重写为 140 项（监控页断言改「无任何持仓入口」+ pgPicks 段），position-snapshot 重写为三页分离快照
- accept-online.js 重写 V3 断言块 + 新增 SIGNAL-CORE/pgPicks 验收

**上线**：console.html 已重新发布（链接不变），在线验收 53 过 / 5 失败——失败全是「云端文件待上传」（signal-core.js/signals.js/screener.js/review.js 404，需 GH_TOKEN；本地网络对腾讯日线临时 501，非回归）。fng-validate 的 fatal 是网络 HTTP 501，与改动无关。

**待办**：用户提供 GH_TOKEN 后 `node tools/upload.js` 上传 5 个云端文件 → dispatch signals.yml 首跑 → 生成 picks-history.json 后推荐页才有真数据。
