# 2026-09-19

### 主力博弈监测系统（mobile_monitor）· Step 4/5 收尾与验收（凌晨，接 09-17 会话）
- test_datasource.py 6/6 全绿：FLOW_HOSTS 双域回退生效，000155 真实资金流经 push2delay 成功（守恒成立、close 与腾讯一致，source=real）。
- 新增 backtest_real.py：东财真实历史资金流（4/24~5/08 验收窗口 + 3/02~5/08 扩展窗口）→ backtest.run_backtest。⚠️ 沙盒网络探测：push2his/push2 代理限流、push2his1/2 反爬页、push2delay 仅最新 1 行（beg/end 被忽略）→ 真实历史序列不可得，脚本失败即退出不伪造；网络放开重跑即完成该项；当前以交付包留存真实快照口径（领先性 3.0 交易日）为准（integration 断言通过）。
- 三接口契约验证（pip 装 httpx，TestClient）：先 run_local.run_pipeline(dry) 灌内存 store → serve:app 三 GET。全绿：字段名按 mobile_ui/README、6 态枚举、卖出优先排序、trend 等长、观望日 signals 空数组。双票 source=real。
- 全量回归 pytest 19 passed + integration 4/4；验收报告《主力博弈监测系统-验收报告.md》（工作区根目录）；临时探针/日志清理。

### 用户澄清「线上没变化」（01:3x）
- 用户以为持仓股主力/散户判定提醒改了。澄清：线上 stock-alert 零改动——透视只在持仓页展示不推送，推送仅 MA5_STREAK_EXIT+条件提醒；mobile_monitor 是独立未部署新工程。给出三条路（接透视 / 部署新系统 / 不动），用户未选。

### V3 多源一致性增量（元宝终版指令，02:1x）
- 指令要点：多源共识 verify（AGREE_RATIO=0.66/MIN_SOURCES=2 勿改）+ 冲突降级 + 连续3天冲突且20日涨>15% → 趋势跟随；app.py/alerter.py 保留原样。
- 新增：core/{v3_multi_source,v3_engine,v3_runtime}.py、datasource/v3_data_provider.py、conftest.py（注入父目录 sys.path 支持 tests 里 mobile_monitor.core 包导入）、tests/{test_v3_multi_source(指令原文),test_v3_engine,test_v3_provider}.py。
- 适配点（指令与工程差异）：①无 scheduler.py → 补丁落 run_local.py run_pipeline（每日任务真身）；②指令 conflict_days=0 伪代码单次运行到不了 3 → v3_state.json 跨日持久化（同日幂等）；③占位 API_URL → 复用已验收真实链路（腾讯快照+东财资金流+腾讯日K，agree=与多数方向一致，平票=无共识→downgrade）；④工程根有旧 datasource.py（aiohttp 稿）抢导入 → datasource/ 加 __init__.py 升 regular package（优先级高于同名 .py），旧文件未动；⑤V3 结论为增量层：推送正文新增「V3 多源核验」段 + [V3] 日志，不覆盖既有 signal 契约。
- 验证：pytest **26 passed**（19 旧 + 7 新）；真实链路双票 verdict=normal、conflict=0（000155 20日-3.1% / 603026 -6.3%）、v3_state.json 持久化幂等；--once --dry 推送预览含 V3 段。app.py/alerter.py 零改动。

### V3 最终补丁（元宝第二版指令，02:3x）——引擎升级 dict 输出 + scheduler.py 落地
- v3_engine.py **覆盖**为指令新版：`run(verdict, conflict_days, price_20d_return) -> dict`（趋势持有 conf0.7 / 观察 conf0.2 / 主信号 confidence=verdict）+ `price_tracking_mode` 标志。指令两处断点适配（v3_multi_source.py 原样不动）：`MultiSourceVerdict` 不存在→`import Verdict as MultiSourceVerdict`；`verdict.confidence` 字段不存在→`_confidence()` 兜底（显式优先，weight=1→0.8）。
- 新建 **scheduler.py**（指令的 scheduler 落地，run_local --serve 仍是调度宿主）：`V3StateDB`（db.get/set 形状→v3_state.json）+ `daily_job()`（conflict_days 跨日+1/归零/同日幂等，单一状态真源）；core/v3_runtime.py 精简为「拉数据+verify+ret20 → daily_job」，状态读写从 runtime 移入 scheduler（单向依赖防循环）。
- run_local.py v3_lines 拼接改 dict 版（state+action）；tests/test_v3_engine.py 重写为 dict 断言（三态+标志置位）；新增 tests/test_scheduler.py（跨日累计 1→2→3→趋势持有、同日幂等、共识恢复归零，monkeypatch STATE_PATH 隔离）。
- 验证：pytest **27 passed**；真实链路 dict 输出 `{"state":"🔴/🟢 主信号","action":"见价格+资金合成","confidence":0.8}`、conflict=0（同日幂等）；dry 推送 V3 段正常。app.py/alerter.py/v3_multi_source.py 零改动。

### ★ 把 V3 判定显示到线上持仓页（用户要求，03:0x — 替换原筹码透视的行为块）
- 需求：「把这套系统的逻辑输出结果显示到持仓股页面，替换掉之前的持仓主力散户行为逻辑」。判定：纯前端可行——chipLiveFrom 已证明东财 JSONP 在浏览器可直连，无需云端 Python workflow。
- console.html 改动（同一文件 Edit 严格串行，共 8 次）：G 加 v3Map/v3Loaded/v3Err/v3Busy/v3Sig；新增 V3 模块（V3_AGREE_RATIO=0.66 / V3_MIN_SOURCES=2 / V3_BARS=40 / V3_LS='sa_v3_state_v1'、v3sign/v3CloseAt/v3StateLoad/Save/v3Vote/v3Ensure/v3BriefHtml/v3DetailHtml/demoV3Data）；CSS 只加 .ev-line 的 .v3-good(蓝)/.v3-bad(橙)，其余全复用 .chip/.st/.bh/.cd-*。
- 三源：源1 当日涨跌（quoteMap.chg，零请求）/ 源2 当日主力净额（push2his fflow fields2=f51-f56，f52=主力元）/ 源3 20 日趋势（fetchDailyBars 40 根，bars[-1] vs bars[-21]）。冲突计数落 localStorage（同日幂等、跨日 downgrade+1、共识恢复归零、源不足不动计数）。trend 条件：冲突≥3 天 且 ret20>0.15。
- 三处渲染替换：持仓卡摘要 1029 `ma5Html + v3BriefHtml`、展开区 1043 `v3DetailHtml`、renderDash 持仓卡（含标题「持仓 · 主力博弈」）；renderDash 末尾 chipEnsure() 后挂 v3Ensure()（chipEnsure 必须留——ma5Cache 管道依赖它）。**旧 chipBriefHtml/chipDetailHtml/chipEnsure/demoChipData/chipLiveFrom 全部保留不删可回退**。
- ⚠️ 踩坑（真 bug，差点静默失效）：fetchDailyBars 经 SignalCore.normBars 后是 `{d,o,c,h,l,v}` **对象数组**，写 `bars[i][2]` 拿不到收盘价 → 源3 恒失效。已加 `v3CloseAt(bars,i)` 兼容对象/原始数组两种形状。**改任何 K 线取值前先确认 normBars 输出结构。**
- 诚实性：缺源兜底 observe（不上冲突计数）；若是降级出结论，cd-note 明说「有数据源未取到…已按可得 N 个源核验」。
- 测试同步（改模型 = console.html + build-preview 注入 + 测试期望，缺一不可）：build-preview 注入 `G.v3Map = demoV3Data()`（自检 17→19）；position-dom-test 387/0（③ 段断言全换 V3，新增 v3Vote/v3sign 内核锁死三态与平票下修、v3Ensure 幂等、旧函数保留可回退）；accept-online 透视段改 V3 断言（常量/三源/趋势条件/localStorage 幂等/挂载顺序 + 旧渲染调用已下线但函数保留）；position-snapshot 注入 v3Map + 断言改「持仓 · 主力博弈」。顺手修一处**与本次无关的过时断言**：快照 pickCount 期望 2 实际 3（弱转强观察池也用 .pick 类）→ 按有无行内 style 区分推荐/观察池。
- 回归：run-offline-regression **全部通过**（build-preview 19/0、position-dom-test 387/0、chip-test 89/0、signal-core 147/0 等）；position-snapshot ALL GREEN。
- ⚠️ 未验证项：源2（东财 fflow）在本机 node 沙盒 fetch failed（网络限制），浏览器走 JSONP script 标签，需上线后实测确认；源1+源3 可用时仍能出结论（≥MIN_SOURCES）。

### ✅ 控制台已发布（03:3x，用户明确要求）
- `workbuddy_sites_deploy` 更新同一应用（`entryHtml:"console.html"`，domainPrefix `stock-alert-console`，updateExistingApp）→ 链接不变 https://stock-alert-console.app.workbuddy.host/ ，verified=true。
- 发布后 accept-online 线上验收 **161 通过 / 0 失败 ALL GREEN**（含本轮新增的 V3 全部断言：常量/三源/趋势条件/localStorage 幂等/挂载顺序/旧渲染调用已下线但函数保留），证明线上就是最新代码。
- 上线后待用户实测确认的唯一项：东财资金流源2 在手机浏览器 JSONP 是否真通（不通则退化为源1+源3 两源投票，详情块会明示缺源）。

### ★ V3 展示层改版：说人话（用户两轮反馈：口语化 + 元宝场景矩阵指令）
- 诉求：不要「主信号·向上 / 多数源向上」这类机器腔，要看懂主力在干什么 + 我该怎么办；指定词：弱回流/观望/主力回流/良性回踩/撤退嫌疑/高位派发/低位吸筹/主力建仓/放量突破。
- 元宝指令（保留原数据接口，**仅改展示层**，算法零改动）：三源映射为交易行为（源1 价格态度=拉升/回落/收平、源2 资金动作=吸筹/派发/回补/出货、源3 中期底仓=建仓区/趋势上行/派发后/阴跌）；五类场景写死文案；第4条明确「置信度与冲突逻辑保持原算法不变」。
- 实现（console.html 串行 Edit）：新增语义层 `v3SrcNote(i,v)` / `v3Head(code)` / `v3Sub(code)` / `v3Advice(code)` / `v3AdvShort(hd)` —— 只负责「怎么说话」，v3Vote/v3Ensure/常量/冲突计数一字未动。12 个场景标签：放量突破/弱回流/主力建仓拉升/回补吸筹/拉高诱多/高位派发/良性回踩/低位吸筹/撤退嫌疑/高位出货完毕/主力持续派发/观望 + V3 三态（趋势持有/观望/待确认）。
- ⚠️ `v3Advice` 刻意**不给目标价**（目标价是预测，无数据支撑），只讲已发生的事实：调 `holdingsOf()` + `PC.pnlOf` 带出成本与浮盈/套牢四档文案；反向断言锁定 `indexOf('13.2') < 0`。
- 仪表盘 V3 行由「主信号·向上」改为 `hd.t + ' · ' + hd.a`（如「🟡 回补吸筹 · 短线可拿，中期仍弱，不追高」）。
- 踩坑两处：①测试里给三安补行情写成 key `'sh600703'`，而全站统一用东财 secid（`secidOf` → `1.600703`，priceAt 同理）→ 看似 v3Advice 失效，实为测试写错；②**演示数据自洽**：dirs 是 chg/mainYi/ret20 经 v3sign 算出来的，原先 600036 写 dirs[2]=0 却给 ret20=+18.7%，页面会同时显示「20日走平」和「趋势上行 +18.7%」。已改：600036=[0,-1,1] chg 0.00；600733=[1,-1,null] srcs=2 err='日K不可得'（顺带演示缺源如实告知）。**新增断言逐只校验 demoV3Data：dirs[i]===v3sign(值)、srcs=有效源数、少了源必须写 err。**
- 顺手：详情块 cd-desc 原先把主标题又印了一遍（徽章已有一个）→ 去掉重复；源1 chg===0 文案由「抗跌」改「收平」（0.00% 说抗跌不通）。
- 测试：build-preview 19/0；**position-dom-test 441/0**（新增三安 600703 用例 + 12 条场景矩阵 + 演示数据自洽 + 不给目标价）；accept-online 新增 6 条展示层断言 + 三源标签断言同步为 ①②③ 写法，**这 7 条在发布前必定挂红**（线上还是上一版），发布后应全绿；run-offline-regression 全通过；position-snapshot ALL GREEN。

### ✅ V3 展示层已发布上线（04:2x，用户「发布」）
- `workbuddy_sites_deploy`（`directory=D:/mywork/stock-alert-console`、`entryHtml:"console.html"`、`domainPrefix "stock-alert-console"`、`updateExistingApp`、`language:static`）→ 链接不变 https://stock-alert-console.app.workbuddy.host/ ，verified=true。
- 发布后 accept-online **167 通过 / 0 失败 ALL GREEN**，含 md5 与本地字节一致 → 线上就是最新代码；发布前那 7 条红（展示层 6 条 + 三源 ①②③ 标签）全部转绿，验证闭环成立。
- 线上待用户实测：东财资金流源2 在手机浏览器 JSONP 是否真通（不通 → 两源投票 + 详情块明示缺源）。

### V3 价格结构子模块 v2 集成（桌面包 v3_price_structure_FINAL.zip，试用分支）
- 包内 `WORKBUDDY_UPDATE_FINAL.md` 要求「替换 v3_engine.py 里的 calc_price_structure()」。核事实：**本工程 core/v3_engine.py 是 dict 输出版，内部根本没有 calc_price_structure()，也没有行级价格结构打分**。按「新增 + 单一替换点」落地：`core/v3_price_structure_v2_final.py`（交付模块逐字未改）+ `core/v3_price_structure.py`（薄适配：calc_price_structure / is_sector_resonant 默认 False / resolve_signal 透传 + 回填三字段）；**v3_engine.py 一行未动**。新增 `tests/test_v3_price_structure.py` 8 条离线单测 → pytest **35 passed**（原 27）。
- 模块自测先跑：`test_final.py` 4 类场景全通过（VR6 共振→跟进+exempt；VR6 无共振→待确认；诱多→不操作；破位→减仓）。⚠️ `__main__` 样例 VR=2.94<3.0 压根没进天量分支，两行 `sector_exempt` 都是 False，与 README 描述的预期不符（样例数据问题）。
- ⚠️ **验收红线「牧原 2024-09-23 = 跟进 + sector_exempt」不成立**：腾讯前复权 −0.90% / 新浪不复权 −0.83%（双源交叉核对）那天是缩量小跌 VR=0.69，猪肉板块 0/10 涨停 = 0%。VR<1.2 连天量分支都没进；即便强塞 sector_hot=True 也先撞 sell_bias → 减仓。**没有为让它变绿而造数据**，已给用户三个选项（换锚点日 09-27/09-30、给他的原始 row、调阈值）。
- 月级样本（真实日K，整月逐日跑）分布：涪陵榨菜 25-11 跟进12/减仓8（11-19 死叉后转减仓）；26-01 跟进12/减仓8 月末跟进 ✅；川能 25-11 跟进13/中性2/减仓5（主升段全跟进，−9.96% 崩盘后转减仓属正确）；新希望 26-09 跟进9/减仓4/待确认1（09-04 涨停 VR4.71→待确认，随后 6 连阴 ✅）；石大胜华 26-09 减仓12/跟进1/不操作1（09-18 诱多过滤 ✅）。涪陵另有电力 600452 对照（25-11 跟进8，26-01 跟进18/待确认1，01-12 VR3.57 无板块数据→待确认）。
- ⚠️ 待用户决策的行为：**增强3 sell_bias 硬条件会误伤底部反转首日**（牧原 24-09-24 +8.33%、石大 09-08~09-10 反弹，因 close<MA20 / MA5<MA10 被判减仓，MA 修复后才转跟进）。是否给「pct≥7% 且 VR≥2 且收盘上穿 MA5」开例外由用户定，**未擅自改**。
- 工具：`_recon/v3ps_final/`（fetch_kline.py 拉腾讯前复权并缓存、regress.py 跑样本、REGRESSION_RESULT.md 报告）；跑 Python 用托管环境 `C:/Users/29086/.workbuddy/binaries/python/envs/default/Scripts/python.exe`（有 pytest/httpx；系统 3.14 有 requests/pandas/akshare 但无 pytest）。
- 状态：**试用分支未合并主线**，等用户确认。

### v2.1 反转首日豁免（用户「开例外」批准，18:1x）
- 规则：放量大阳（pct≥7% 且 VR≥1.5）且收盘站上 MA5 → 不判 sell_bias、不扣 12 分，置 `reversal_exempt=True`；诱多过滤仍是最高优先级。常量 `PCT_JUMP=7.0` / `VR_REVERSAL=1.5` 在模块顶部可调。
- ⚠️ 阈值教训：我口头提的「VR≥2」会漏掉自己举的例子（牧原 24-09-24 VR=1.57）→ 实际取 1.5，**给用户提阈值前先用真实数据验一遍**。
- 真实数据复核：牧原 09-24 减仓(19)→跟进(31, exempt)✅；09-23 仍减仓✅；新希望 09-04 天量待确认不因豁免消失✅；石大 +2.1% 不豁免✅；诱多过滤保留✅；6 样本整月分布逐字未变（无误伤扩散）。
- pytest **37 passed**（27+8+2）。单测坑：造「量不够」样例时 VR=0.79<VR_THIN(0.8) 会掉进诱多过滤分支返回 force_no_trade 而非 sell_bias，样例量要落在 (0.8,1.5) 区间。
- 红线（牧原 24-09-23 板块共振）仍不通过——前提与公开行情不符，等用户换锚点/给原始 row/调阈值。REGRESSION_RESULT.md 已更新至 v2.1。

### v2.1 定稿 + 合并主线（用户三连决定：换930 / 电力 / 合并，18:2x）
- 用户定稿：涪陵=**涪陵电力 600452**；红线锚点=**2024-09-30**；批准**合并主线**。
- 回归集定稿 6/6 全符合：电力 25-11 跟进8/减仓12、26-01 跟进18/待确认1（01-12 VR3.57 待确认→次日转跟进，两阶段✅）、川能 25-11 ✅、新希望 26-09 ✅（涨停 VR4.71→待确认）、石大 26-09 ✅（诱多过滤）、**牧原 09-30 跟进(31.0) → 红线✅**（VR=2.44 无天量，sector_exempt=False 属正常——无天量无需豁免；豁免逻辑由单测锁定）。
- 合并方式：接线点=**run_local.run_pipeline 的 V3 增量层（编排层）**；v3_engine/v3_multi_source/scheduler 仍一行未动。新增纯函数 `build_row`（<21根返回 None 不评分）/`price_structure_for`（K线可注入）/`apply_structure_ruling`（裁决优先级：force_no_trade 覆盖一切买入 > sell_bias（叠加主力净流出→强减仓，流向未知不放大）> pending_confirm 只降级主信号 > sector_exempt 仅标注 > 其余维持原结论）。**不回写 store 里引擎原信号**（审计可回溯）。
- 端到端 `--once --dry` 实测：603026 主信号被诱多过滤压成「⛔ 不操作」、000155 →「🟠 减仓偏置」——裁决优先级真实链路生效；东财 push2his 代理失败自动回退 push2delay（source=real）。
- pytest **43 passed**（27+8+2+6 合并主线单测）。
- 模块位置：`mobile_monitor/core/v3_price_structure.py`（接入口）+ `core/v3_price_structure_v2_final.py`（v2.1 评分）；回归 harness `D:/mywork/_recon/v3ps_final/regress.py`（kline_cache.json 缓存真实日K）。

### ★ 用户纠偏 + 价格结构 v2.1 上线持仓页（18:3x）
- **用户纠偏（重要认知修正）**：这套 V3（多源核验 + 价格结构）是**昨天做的持仓股监控更新的延续，不是新的独立项目**——mobile_monitor 是引擎、控制台持仓页是线上显示，属同一体系。此前我想把 mobile_monitor 当独立项目新建应用部署，被叫停（那次部署因启动失败未上线任何东西，控制台无影响；为独立部署临时建的 run_server.py 已删，requirements.txt 保留）。
- 正确的「发布线上」= **把价格结构 v2.1 移植进控制台持仓页 V3 卡片，updateExistingApp 更新现有应用**。已完成：
  - console.html 新增 PS 模块（v3PsRow/psCalc/psBias/psRuling，常量 PS_VR_LOW=1.2/PS_VR_HIGH=3.0/PS_RET20_CAP=15/PS_VR_THIN=0.8/PS_PCT_JUMP=7.0/PS_VR_REV=1.5），**与 Python 引擎逐条同口径**（含 v2.1 反转首日豁免）；复用 v3Ensure 已抓的同一份日K（零额外请求），<21 根 psErr='K线不足' 不评分。
  - ⚠️ **线上无板块数据 → sector_hot 恒 false（保守）**：VR>3.0 一律走待确认，不误判买入；豁免仅演示数据/单测体现。
  - 显示：详情加「价格结构 / 量比VR / 结构裁决」行 + 备注行；硬裁决（⛔不操作/🔴强减仓/🟠减仓偏置/⏳待确认）在摘要行主标题后加徽章；无硬裁决标题不被改写（t 空 = 以 V3 为准）。
  - 裁决优先级移植 apply_structure_ruling：force_no_trade 覆盖一切买入 > sell_bias（叠加 mainYi<0 → 强减仓）> pending_confirm 只降级 state==='normal' > 其余维持 V3。
  - demoV3Data 四只都带 ps（600733 诱多 → ⛔ 覆盖「观望」演示硬裁决），延续自洽纪律。
- 测试：position-dom-test **454/0**（新增 ③i-3 九条 PS 断言；踩坑：JS 里 `const诱多Row` 无空格会把 const 吞进标识符 → 崩溃，标识符一律用 ASCII）；run-offline-regression 全通过；position-snapshot ALL GREEN；accept-online 新增 4 条 PS 断言。
- **已发布**：workbuddy_sites_deploy updateExistingApp（A股监控控制台），链接不变；发布后线上验收 **171/0 ALL GREEN**。

### 期指贴水 · 用户定稿 + 首页卡片排序（19:0x）
- 期指贴水：**维持现状，不切换**（用户拍板）。脚本 fng-basis-validate.js 保留可重验。
- 首页仪表盘排序（用户定稿）：**持仓 · 主力博弈 提到第一位，「距触发最近」下移到持仓之下**，今日推荐仍第三。改 renderDash 卡块顺序 + #dash 首屏骨架顺序（骨架标题顺手从过时的「持仓 · 当日收盘评测」改成「持仓 · 主力博弈」）+ 注释编号。
- 测试坑：position-dom-test 首屏骨架断言硬编码「距触发最近」打头 → 改为断言「持仓 · 主力博弈」打头（454/0 全绿）。
- 状态：本地全绿，**尚未发布**（等用户说「发布」，发布授权不跨轮继承）；线上验收当前 170/1，唯一失败项即 md5 未一致（=未发布的证据）。

### 期指贴水因子 · 回测验证否决（18:5x，补完 09-18 的承诺）
- 背景：09-18 只做了数据探针没做回测；用户问起 → 补完。脚本 `_tests/fng-basis-validate.js`（口径与 fng-dbd-validate.js 一致：前瞻 60 日分组，门槛=单调 且 区分度≥现行）。
- ⚠️ 数据源变更：新浪老符号 `CFF_RE_IF0` 已返回 null，**须用 `symbol=IF0`**（返回 `var t=([...])`，2017-01-17 起 2349 天）。basis=(IF0收盘−沪深300)/现货×100，20 日均值平滑换月跳变，滚动 252 百分位。
- 结果（fwd 用沪深300，与现行基线同口径对比）：现行5因子 区分度 3.73% 单调；方案A(basis15) 2.91% ❌；方案B 3.40% 且**单调性破坏** ❌；方案C(basis10) 3.09% ❌ → **三个方案全部未过门槛，不切换**。
- 有意思的诊断：**贴水因子单独 区分度 4.54%（单调，>现行 3.73%）**——但并入 5 因子后被稀释，与动量/两融冗余，与股债差额被否同款教训。基线从 5.36%→3.73% 是因为 fwd 标的从全指换成沪深300（同脚本内对比口径一致，不受影响）。
- 状态：**维持现行 5 因子**；脚本保留，样本变长可重验。可选项：贴水作为恐贪页参考线展示（不进分数）。

### 大盘风格驾驶舱（S1）· 全链路做完，本地全绿未发布（20:xx）
- 用户需求：监控大盘风格（僵尸/主升浪/妖股/轮动/抱团/小微盘）+ 每日优选 ≤3 只「资金最看好且非顶部结构」+ 左右滑轮播 + 可删除；**龙头战法保持独立页**；首页三卡结构不动（用户：高频需求仍是恐贪+持仓评断）。
- 云端新文件 `stock-alert-cloud/style.js`（STYLE_VERSION=S1）：七档判定（defense>theme-run>short-term>rotation>crowd-large>small-cap>mixed）+ 操作指南话术写死 + 优选（主力连续净流入 ≥2 日 + 板块资金流前 20 + PS v2.1 硬规则闸门（psGate/psRow 与控制台 psCalc 同口径，云端多板块共振豁免 sectorHot）+ 冰点/退潮不出票 + 统一止损 -5%）→ 写 style-history.json（days 保留 120 天）。screener.js 追加导出 getJson/fetchPool/fetchTradingDates/fetchBars/fetchQuotes（纯增量）。
- signals.yml 加 `node style.js` 步骤 + git add style-history.json。
- **修复 screener.js 真 bug**：fetchTradingDates URL `day,,n` 少一逗号 → gtimg 返回 param error → **情绪周期在生产上一直静默降级**；改 `day,,,n`。探测结论：ifzq 指数参数必须 `code,day,,,根数,qfq` 五段。
- ⚠️ 数据源：**push2/push2his 本机 socket hang up，必须走 push2delay**（clist 正常；fflow daykline 只存最近 1 天）→ fetchStockFlowHist 先 push2his 后 push2delay 兜底，flowDays=1 时按当日口径放行并如实降级标注（passFlow）。
- 控制台：新页 #pgStyle（sty-* CSS 约 45 条）+ sheetMore 第一项 rowStyle + STYLE 模块（styleDemoData/styleLoadRaw/styleEnsure/renderStyle/openStyle）+ 优选 scroll-snap 轮播带页点 + 删除=localStorage `sa_style_ignored_v1`（日期|代码，本批不再显示，云端不动）+ 「＋记持仓」复用 openAdd('holding')+srSelectStock 预选 + 取价并集并入 styPk + 行情回来重渲。
- 配色：风格档 跟=蓝/提醒=琥珀/躲=橙/观望=灰（避开红绿）；价格仍红涨绿跌。
- ⚠️ 编辑器坑再现：Edit 报成功但未落盘发生 3 次（test fixture、style.js sectors、rowStyle 监听）——**批量编辑后必须跑核对脚本**（`_tests/_cache/check-style-edits.js`）；CSS 插入截断过 .vtab.on .n 规则（原值 rgba(255,255,255,.55)，从 _preview 旧副本找回）。
- 测试：style-engine-test 50/0（纯函数全覆盖+passFlow 降级）、position-dom-test **499/0**（+28 条驾驶舱断言）、build-preview ALL GREEN（注入 STYLE.data=styleDemoData）、全量回归全绿（style-engine-test 已入清单）、快照 ALL GREEN。
- 状态：**双端已全部发布完成，线上验收 ALL GREEN**。

### 驾驶舱发布踩坑（20:3x，务必记住）
1. **tools/upload.js 有固定上传清单**：新文件必须手动加进去（第一次跑 style.js 根本没传，只上了 screener.js+signals.yml）。已加 `['style.js', ...]`，排在 screener.js 之后（依赖其数据管道）。
2. **signals.yml 的 `git add` 必须含 style-history.json**：漏了的话 workflow 全绿、style.js 也跑成功，但存档留在 runner 里不提交（现象：step 全 success + 仓库 404）。
3. **phantom edit 又出现 3 次**（test fixture / style.js sectors / signals.yml git add / upload.js 清单都被报成功但没落盘）→ 批量编辑后必须逐字核对（`_tests/_cache/check-style-edits.js`）；云端侧则要靠「跑完 upload 看输出 + 用 token 查仓库内容」确认。
4. verify-cloud.js 匿名读 workflow 恒 403（已知噪音）；accept-online 的云端内容核验遇 403 按口径跳过不判失败（已改成跳过，与 needIn 一致）。
- 云端真实产物（runner 首次跑，push2his 可用走严格口径）：2026-09-19 快速轮动市｜周期发酵｜恐贪 39.1｜涨停 78｜高度 4｜主线 通信设备→汽车零部→电网设备｜优选 君正股份 84 / 新易盛 70 / 天孚通信 66。

### 去 V3 标识 + 驾驶舱读出 09-18 收盘（21:0x，已发布）
- 用户两连要求：①持仓去掉 V3 标识 ②V3 旁边的小图标一起去掉 ③驾驶舱空的要显示 09-18 收盘。
- **驾驶舱空的根因**：style-history.json 是 `{v,updated,days:[...]}`，控制台 `styleLoadRaw` 按单日对象读 → `raw.style` 恒 undefined。改为「取 days 最后一天」并保留单日兼容；同时**把 raw.githubusercontent 提到 jsdelivr 之前**（jsdelivr 有 CDN 缓存会拿回旧存档，date 字段滞后一天）。
- **日期口径**：云端 style.js 的 `date` 原记运行日（09-19），改为**记数据所在交易日**（09-18），另加 `generatedAt`（生成日）；去重过滤同步按 dataDate。控制台显示行区分「数据日 收盘更新 · 引擎 S1」+ 生成日。
- 去 V3：可见文案全部改掉（chip-title「主力博弈」、cd-note 口径说明、仪表盘 ev-k 标签、详情「以主力博弈为准」）；**算法层 V3_* 常量/函数一字未动**；注释里的「主力博弈 V3」保留（历史描述，不影响显示）。
- ⚠️ **emoji 清理踩坑（两次）**：第一次正则 `'(🟢|…)\s?` 用字面 emoji，4 字节字符被截成半个代理项 → 页面出现方块/U+FFFD；补救又留了孤立代理项。最终用**码点转义** `[\u{1F7E2}...]/u` 一次性清干净（含注释里 12 处），U+FFFD 与孤立代理项均归零。**教训：处理 emoji 一律用 \u{...} 转义，不要写字面字符、不要按 JS 字符串长度切。**
- 断言纪律：V3/emoji 断言改为**判真实渲染代码**（`chip-title">主力博弈 V3`、`'以 V3 为准'` 带引号、`'🟢` 带引号），不判裸词——否则注释里的历史描述会误伤（踩过两次）。
- 测试：DOM 500/0、离线全量回归全绿、position-snapshot ALL GREEN；**线上验收 169/0 ALL GREEN，md5 一致，date=2026-09-18**。
- 已发布（updateExistingApp，链接不变）：https://stock-alert-console.app.workbuddy.host/

### 核查：散户行为（抄底/割肉）还能不能看到（21:3x，结论：**数据活着、显示已被顶掉**）
- 用户问「还能看到散户是抄底还是割肉吗」→ 查证：
  - **数据链路完整活着**：云端 `chips.js` 每日跑（signals.yml 有 step + git add），`chips-history.json` updatedAt=2026-09-19、17 只、含 `main/retail/stance/chips/flows`；控制台 `chipEnsure()` 仍在 renderDash 末尾调用（①读云端 T+1 ②存档没有的走 chipLiveFrom 东财 JSONP 实时兜底）。
  - **但展示层是死代码**：`chipBriefHtml`/`chipDetailHtml` 有定义、**零调用点**（09-17 改版被 V3 行为块顶替），所以页面上确实看不到。
  - `judgeRetail`（行 5551-5605）散户七态完整：抄底/割肉/观望/获利了结/追高接筹码/慢慢买/磨走/僵持；核心认知（行 4970-4975）：**散户净额恒等于 −主力净额，只看资金流分不出抄底与割肉，必须叠加价格方向**（跌+主力买=割肉 / 跌+主力卖=抄底 / 涨+主力买=获利了结 / 涨+主力卖=追高）。
  - V3 三源投票把「涨跌」与「主力净额」当**两个独立源**投方向票，不做四象限交叉 → 恰好丢掉了「抄底 vs 割肉」这个区分（V3 只说主力视角：放量突破/回补吸筹/高位派发/低位吸筹）。
- 若要恢复：数据现成、`chipEnsure` 已在跑 → **零额外请求**，只需在持仓详情块补一行渲染即可。

### ★ 散户行为行放回（用户定：详情一行 + 主页也要，21:4x，已发布）
- 用户追加要求主页也要有 → 两处都加，**严格遵守「不干扰 V3 逻辑」**：
  - 新增纯展示函数 `retailLineHtml(code)`：只读 `G.chipMap[code].retail`（chipEnsure 已算好的筹码透视：云端 T+1 存档 + 东财实时兜底），**零额外请求、不参与三源投票**，V3_*/v3Vote/v3Ensure/常量/冲突计数一字未动。
  - **持仓详情**：`v3DetailHtml` 里在「结构备注」之后、「操作建议」之前插入 `h += retailLineHtml(code)` —— 完整 tag（散户在割肉）+ `.st.{tone}` 徽章 + desc 说明；缺数据给空态（未核算完=「核算中…」，已核算无数据=「暂无筹码数据」）。
  - **首页仪表盘持仓卡**：`ev-line` 加第三行「散户」，用 `shortTag()` 短标签（割肉/抄底/追高接筹码），class 映射与 V3 行同款（good→v3-good 蓝 / bad→v3-bad 橙 / warn→warn / mute→flat）。**无数据不渲染**（首页保持干净，空态只给详情页）。
  - 演示数据天然可用：`demoChipData` 与 `demoV3Data` 覆盖同一批代码（002415/600519/600733/600036），build-preview 已注入 `G.chipMap = demoChipData(); G.chipLoaded = true`。
- ⚠️ 必须同步改的**旧断言**：position-dom-test 有一条 09-17 遗留的 `notHas(evRow.textContent,'散户','旧的散户行已从仪表盘移除')` → 反转为 `has(...'散户')` + `has(...'割肉')` + `notHas('散户在割肉')`（首页是短标签，不带「散户在」前缀）。这是「改模型必同步测试期望」的又一个实例。
- 新增隔离性断言（防以后误改）：清空 `G.chipMap` 后 `v3Head().t` 与 `v3Vote(dirs)` 结果**必须不变** —— 锁死「散户行纯展示、不参与投票」。
- 测试：DOM **508/0**（+9）、离线全量回归全绿、线上验收 **160/0 ALL GREEN**（md5 一致，chips 存档 stocks=16）。
- 已发布（updateExistingApp，链接不变）。

### ★ 两套规则对比回测（22:0x，研究性，未改任何线上规则）
- 用户问「V3 和之前的主力散户博弈，哪套更能让我站对边」→ 做了真实数据回测（**不进回归清单**，纯研究）。
- **方法**：不复刻规则 —— jsdom 加载 preview，直接调线上同一份 `ChipCore.analyze` 与 `v3Vote+v3Head`，喂同一份数据保证公平。脚本 `_tests/rules-backtest.js` + `_tests/_cache/bt-fetch.js`（数据缓存 bt-data.json）。
- ⚠️ **数据源踩坑与换源**：东财 push2his 在本机被沙盒代理 `127.0.0.1:53821` 掐断（akshare 底层也是它，一并挂）。可用替代：**腾讯 `web.ifzq.gtimg.cn` 前复权日K** + **新浪 `MoneyFlow.ssl_qsfx_zjlrqs` 历史资金流（400 天，只有 r0 超大单）** + 腾讯 `qt.gtimg.cn` f[44] 流通市值（亿元）→ 股本。新浪只有超大单、线上东财口径是「大单+超大单」，**符号一致率 5日 13/17、20日 12/17**（噪声对两套相同，横向比较仍成立）。flows 需拼成东财 klines 形状并守住 small+mid=−main。
- **结果（17 只 × 6440 个交易日·股票点，2025-01~2026-08，样本期是上涨市，基准 10日 +0.8%）**：
  - 方向准确率：**V3 49.7% / 筹码 50.3%**，都在 50% 附近，**差异不显著**（<1 个标准误）。两套都不能当买卖指令。
  - 区分度（跟−躲 10日收益差）：筹码 +0.58pp > V3 **+0.04pp（≈0）**。
  - 按信号操作 vs 买入持有：筹码 +7.9% / V3 +3.8% / 基准 +8.7% → **两套都跑输躺平**（上涨市里喊「躲」就吃亏；V3 喊躲 2025 次占 31%，筹码只喊 632 次占 10%）。
- **场景级才是真结论**（10 日后收益，相对基准）：
  - 最稳健正信号：**散户割肉 surrender +2.5%（4/4 时段为正：+2.9/+4.8/+1.1/+3.0）**、**主力建仓 accumulate +1.7%（4/4 为正）** → 正是这套系统最初的设计意图，且统计显著（上涨占比 58.2%，≈3σ）。
  - 最差：**散户抄底 dip-buy −0.4%**、**主力拉升已获利 lift −0.9%（上涨仅 40.6%）** → 「别追」是对的。
  - **V3 的「主力持续派发」（躲，1219 样本占其躲信号 60%）后续仍 +0.5%，且 3/4 时段为正 → 这个信号基本是噪声，甚至反指**；V3 唯一有效的是「高位出货完毕」−0.1%/上涨 38.6%，但只有 207 样本。
- **诊断（为什么 V3 区分度差）**：V3 源2 用**当日**主力净额（单日噪声极大），筹码透视用 **20 日累计**资金流（平滑）。V3 本质是当日快照，对噪声敏感 → 在上涨市里频繁误报「派发」。
- 用法建议（给用户）：散户行为维度（刚放回的那行）信息量最大，尤其「散户在割肉 + 主力在建仓」组合；V3 的「主力持续派发/拉高诱多」要打折看。两套都只是参考，不是买卖指令。

### ★★ 融合方案 F2（22:3x，两套合一，已在回测里验证；**尚未落地线上**）
- 需求：用户要「去掉缺点、提取有用的，融合成一套」，并强调**避免过拟合、避免区间偶然**，还提示「V3 上涨有噪音，那它的逃顶会不会灵敏」。
- 脚本 `_tests/rules-fusion.js`（数据仍用 bt-data.json）。**防过拟合的四条纪律**：
  1. **不新增任何参数** —— 只复用两套规则已有常量（HI=15% 高位、overCost 25%、V3 价格结构硬裁决）。融合的是「谁在什么条件下说话」，不是调数字。
  2. **样本内定、样本外验** —— 全部调整只依据 2025（内），再用 2026（外）验证。
  3. **不看总体准确率**（会被单边行情带偏），改看：分市场环境 + 极端下跌的精准/召回/lift + 分时段。
  4. 选方案的判据是**分时段为正的时段数**，不是全样本最好看。
- **F2 规则（中期定方向、短期定时机）**：
  - 躲（高置信度）：① V3 价格结构**硬裁决**（诱多 force_no_trade / 破位 sell_bias）② 高位派发（ret20>15% 且 main=lift/distribute）③ 主力已获利 lift ④ 散户追高 chase
  - 跟：① 主力在建仓 accumulate/inflow 且未远离主力成本(<25%) ② 散户割肉 surrender
  - 观望（关键改动）：**主力在撤 / 散户抄底 —— 由 F1 的「躲」降级为「观望」**
- **为什么降级（依据 2025 样本内）**：主力在撤 2025H1 +3.92%、2025H2 +2.61%（后续仍涨）；散户抄底 +4.65%、+1.08% → 都不是有效卖出信号。**躲=卖出代价高，只在稳健证据下才喊**。
- **结果（17 只 × 6440 点）**：
  | 方案 | 跟/躲 10日区分度 | 分时段为正 | 样本外躲准确率 | 逃顶 lift(样本外) |
  |---|---|---|---|---|
  | V3 | +0.05pp | 2/4 | 58.2% | 1.12x |
  | 筹码透视 | +0.59pp | 2/4 | 57.4% | 1.03x |
  | F1 | +1.69pp | 2/4 | 62.8% | 1.26x |
  | **F2** | **+1.71pp(全)/+4.18pp(外)** | **4/4** | **66.7%** | **1.64x** |
  - F2 样本外：跟 1105 个 +1.33%（准 51.8%）／躲 192 个 **−2.85%**（准 **66.7%**）；说躲后 20 日内真跌超 5% 的概率 66.7% = 基准 40.6% 的 **1.64 倍**。
- **回答「V3 逃顶灵不灵」**：**灵**。V3 的硬裁决（价格结构）正是 F2 躲信号的第一来源；「高位派发」2026H1 −10.48%、2026Q3 −5.46%，大跌率 50.3%；「主力已获利」**4/4 时段为负**。V3 的毛病是**上涨市里乱喊躲**（其「主力持续派发」1219 样本后续仍涨），不是逃顶不灵。
- ⚠️ **诚实局限（必须一起讲）**：
  1. F2 只存在于回测脚本，**线上 console.html 一行没改**，落地需用户点头。
  2. **召回只有 12.1%** —— 只能预警约 1/8 的大跌（F1 能预警 38.5% 但噪音大）。精准与召回的权衡，F2 选精准。
  3. **「高位派发」分支在 2025 失效**：2025H1 +1.00%、2025H2 +2.67%（正），只有 2026 两段为负 → 该分支 2/4，不够稳，靠「主力已获利」4/4 撑住整体。
  4. 资金流仍为新浪 r0 超大单代理（与东财口径符号一致率约 75%）；样本仅 17 只、窗口 2025-01~2026-08，且 2025/2026 市场环境差异大（大跌率 19.6% vs 40.6%）。
- **最稳健的两个分支（可单独用）**：主力在建仓 4/4 正（+2.63%）、主力已获利 4/4 负（−2.49%，大跌率 41.7%）。

### ✅ F2 已落地上线（22:4x，用户「做吧，我要准确率，少噪音」）
- console.html 新增 `FUSION_VERSION='F2'` + `fusionVerdict(h)`（与回测 fusion2 **逐条同构**：①硬裁决→躲 ②ret20>15%+lift/distribute→躲·高位派发 ③lift→躲·主力已获利 ④outflow/distribute/driftdown→观望 ⑤dip-buy→观望 ⑥accumulate/inflow：overCost≤25%→跟·主力在建仓，>25%→观望 ⑦surrender→跟 ⑧chase→躲 ⑨其余观望）+ `fusionDesc(fv)`（分支说明，如实带回测数字与「高位派发 2025 曾反向」的警告）。
- **纯展示层**：只读 G.v3Map（ret20/ps.flags）+ G.chipMap（main.key/retail.key/chips.mainCost/price），零额外请求，V3/psCalc/ChipCore/冲突计数一字未动（DOM 断言锁死）。
- 现价优先级：quoteMap 实时价 > chipMap.price（与回测「当日收盘」口径一致）。
- 展示：首页持仓卡第四行 `ev-k">综合`（跟=蓝/躲=橙/观望=灰，act · why）；详情在散户行之后、操作建议之前插「综合研判 → 动作（原因）」+ 说明 + 口径注（含召回仅约 1/8 的诚实声明）。
- **缺数据兜底**：筹码缺失时只有硬裁决能给「躲」，否则返回 null 不渲染（不硬凑）。
- ⚠️ 踩坑：**demoChipData 的 main/retail 缺 key 字段**（真实 toChipCard 有，demo 手工构造漏了）→ fusionVerdict 在演示模式全失效。已修：mk() 加 MK/RK 映射补 key（与真实结构对齐）。教训：**演示数据必须与真实数据结构逐字段同构**，加消费方时先核对。
- ⚠️ 测试期望坑：演示 600519 期望「跟·主力在建仓」实际是「观望·已远离主力成本」——demo 实时价 1565.6 vs 成本 1196 = +30.9% 过了 25% 闸门，**F2 在正确工作**（实时价优先=回测口径）。另「主力成本」notHas 断言改为判 `cd-k">主力成本`（数据格），因融合文案合理提及该词。
- 测试：DOM **527/0**（+19 条 F2 断言：11 分支单测 + 隔离 + 展示 + 缺数据）、全量回归全绿、线上验收 **179/0 ALL GREEN**（md5 一致）。已发布 updateExistingApp，链接不变。

### 买入侧专项对比（22:5x，用户诉求「跟主力建仓买入 > 逃顶，怕抄在下跌中继」；rules-fusion.js 第七段）
- 20日收益 / 中继率（20日内跌超5%）：筹码 accumulate +2.39%/26.1%（n=2964）；V3 低位吸筹 +2.77%/26.9%（n=394）；V3 主力建仓拉升 **+1.11%/31.3%**（中继率比基准 28.1% 还高，最差）；V3 良性回踩 +2.41%/27.8%；基准 +1.57%/28.1%。
- **黄金组合「建仓+散户割肉」：20日 +5.45%、中继率 19.9%、最差5%经历 -14.84%（全表最优）**，n=191（样本不大需注明）。
- **「假建仓」陷阱实锤**：建仓+散户抄底 → 10日 −0.47%、中继率 **30.1%**（比基准还高）＝「以为主力在建仓、实际下跌中继」；建仓+散户未抄底 → 20日 +2.63%、中继率 25.6%。散户行为闸门把两者分开，这正是 F2 dip-buy 观望分支的价值。
- 结论：**买入口径筹码透视 > V3**（V3 建仓类信号收益 +0.7~0.9% 全部低于筹码 +1.4~1.6%，且「主力建仓拉升」中继率最高）；V3 的独特价值在硬裁决管「躲」，不在买入。F2 已是最优组合。
- 可选未做：UI 上把「建仓+散户割肉」标记为增强买点（黄金组合样本仅 191，等用户决定）。

### 用户反馈「持仓全是躲、综合与主力矛盾」→ F2 重大缺陷复盘与 F3 修复（20:0x-23:xx）
- 实锤复现：16 只监控股 15 只命中 ps 硬裁决躲（14 sell_bias + 1 force_no_trade）。根因：sell_bias 含 `MA5<MA10` 单条件即破位，普跌周全市场触发 → 个股零区分度，还盖掉黄金组合买点。
- **重大发现：F2 上线时回测脚本漏调 psCalc（只调了 v3PsRow 评分行），价格结构硬裁决分支从未被回测过**。此前宣传的 66.7%/1.64x 全部来自筹码侧分支（高位派发300+主力已获利230+散户追高29=559 条），与线上行为不符。
- 补齐口径后真实回测（6440 点）：F2 硬裁决触发 3699 次（57%），触发后 10 日 +0.87%、躲对率 47.6%（比硬币差）。
- **F3**（不加参数，只改覆盖逻辑，依据 V3 自身 psRuling「破位+派发=强减仓」语义）：①force_no_trade（诱多，罕见 n=52）保留硬躲；②sell_bias 只有主力同向在撤(outflow/distribute)才升躲「破位+主力派发」；否则只挡买入（跟→观望「破位暂缓买入」）；筹码缺失+破位→「破位待核验」观望。样本外：躲 4086→791 条、精准 62.5%（1.54x）、躲后10日 −2.94%；放行的「主力在建仓」20 日 +3.94%、中继率 21.8%，黄金组合 +5.50%。
- console.html：FUSION_VERSION='F3'、fusionVerdict 重写、fusionDesc 新增三分支文案、详情免责声明数字更新；position-dom-test（532 绿）与 accept-online 断言同步。
- fusion-tracker/run.js：fusionEval 同步 F3；修 isFinite(null)===true 统计 bug（未回填的 null 被当 0）；实测幂等（重跑跳过 17）、--report 可出月报。历史 fusion-history.json 首批 17 条即 F3 口径（09-18 数据）。
- 定时任务已建：每日 20:30 记录回填 + 每月 1 日 09:00 出月报（automation_update，两个 recurring）。
- ⚠️ F3 已过全量回归但**尚未发布上线**（发布须用户明确要求）——用户确认后用 workbuddy_sites_deploy entryHtml:'console.html' 更新。
- ✅ F3 已发布上线（用户确认）：workbuddy_sites_deploy updateExistingApp + entryHtml console.html，链接不变 https://stock-alert-console.app.workbuddy.host/ ，线上/本地 md5 一致；线上验收 178/1 —— 唯一失败是腾讯日线探测（沙盒高频探测触发腾讯 waf 501 反爬页，属环境抖动非产品问题：同接口低频请求正常，浏览器端不受影响）。
### 用户纠正「09-18 是 4000 家上涨不是普跌」→ F4（23:0x-23:4x）
- ✅ 用户说得对：实测 09-18 上证 +0.94%、17 只监控股 13 只上涨（002471 涨停）。真实情况：09-10~09-15 指数 3951→3864 回调，09-16 起反弹。**此前我写的「普跌周」表述错误，已更正**——正确诊断：F2/F3 信号被慢变量（MA5<MA10 余波、20日累计净额）主导，反弹两天了还没消化。
- **F4**（仍不加参数）：破位(sell_bias)不再挡买入——回测证明被 F3 拦下的「建仓+回踩」恰是最强买点（回踩·建仓 n=851 20日 +4.76% 中继率 19.4%、回踩·黄金组合 n=153 +5.50%，**均 4/4 时段全正**，比非回踩建仓 +1.15% 更好）。F4 = ①force_no_trade 保留硬躲（n=52 样本少，desc 如实标注）②破位+主力同向撤→躲「破位+主力派发」③破位+筹码侧跟→保留跟+pullback:true 标注（首页/详情显示（回踩））④其余维持筹码侧。回测：样本外跟 10日 +1.34%（F3 +0.02%）、躲精准 62.5% 不变、跟后大跌率 25.8%<基准 28.1%、跟−躲分时段 3/4 正（−1.05/+2.17/+4.21/+2.29）。
- ⚠️ 新浪日K备源（不复权）会让 ChipCore 筹码判定整体畸变（实测 mainK 全变 lock/flat）——tracker 已加降质保护：ksrc!=='tx' 一律跳过不入库。今天腾讯 WAF 封沙盒 IP（高频探测触发 501 反爬页），F4 信号待明天 20:30 每日任务补录。
- console.html FUSION_VERSION='F4'、tracker fusionEval 同步、DOM 测试 531 全绿、回归全部通过。待发布。
- ✅ F4 已发布（用户确认），链接不变，线上/本地 md5 一致。

### 诚实性自查（用户问「整个过程有无诚实性问题」）
- 结论：**有 4 处**，已全部自曝/已修；另 4 项局限仍在，必须在回答里说清，不能靠数字好看糊过去。
- 已承认并修复的：①F2 的 ps 硬裁决分支上线前从未被回测（`rules-fusion.js` 只调 `v3PsRow` 没调 `psCalc`），真实精准率 47.6% 而非宣称值；②「09-18 普跌周」是错的（上证 +0.94%、13/17 上涨），用户当场纠正；③F3 全观望是我自己造的新错（破位门禁拦掉回踩买点）；④F4 上线前未做「日频噪音」自检，事后实测结论平均 5.3 天切换、10 日内翻转 77.7%，说明 F4 根本不适合中长线——这是 S1 立项的直接原因。
- 做得对的克制：深跌+缩量+资金净流入 60日 +33.44% 因 n=22 太小，**只写在报告里、拒绝进控制台**（node 复核：console.html 中 33.44 出现 0 次）。控制台所有回测数字均带样本量（n=112、3/3 时段正）与反向警示（派发完毕 2025 两时段反向）。
- 仍存在的局限（必须如实告知）：样本仅 17 只×2.7 年；资金流用新浪 r0 超大单代理（与东财大单+超大单方向一致率约 75%）；派发完毕态 2025 反向；S1 尚未用用户自己的实盘数据验证（建议 tracker 扩展记录周/月级信号，约 3 个月后可验）。
- S1 已完整改造但**用户选择先不发布**（线上仍是 F4）。遗留：定时任务名称仍写「F3信号追踪」，实际跑的是 F4 逻辑，标签待改名。
