# 2026-09-20

### F4 自检（用户诉求：中长线关键位置，不是每天出信号）
- 自检脚本 `_tests/f4-selfcheck.js`（A日频噪音 / B位置缺失 / C周月三态原型 / D关键位置交叉 / E底部形态），报告 `_tests/f4-selfcheck-report.md`。
- 实测结论：①F4 结论平均 **5.3 个交易日**切换一次，喊跟后 10 日内翻转 **77.7%** → 日频就是噪音，用户诉求成立。②缺位置维度：同为「跟」，pos>0.7 高位 60日 +3.22%/跌超5% 36%，vs 中位 +6.52%/31%，vs 低位 +6.57%/25.8% → 高位需降级。
- 周/月三态原型（日K聚合周K，零新接口）：①上涨乏力·派发完毕 触发 3.4%、60日 −5.79%、跌超5% 54%（⚠️2025 两时段反向，仅 2026H1 −12.46% 成立，只作提示）；②**下跌衰竭·无资金是陷阱不是底**（60日 −1.36%、跌超5% 42%）；③主力建仓 30.9% 太频繁需叠加门槛。
- 最强关键位置：**主力周资金净流入 + 低位(pos<0.4) + 周线多头(wMA5>wMA20)** n=112，20日 +6.62%、60日 **+14.80%**、跌超5% **17.9%**、分时段 3/3 全正。深跌+缩量+无资金 = 明确陷阱（60日 −8.74%、58.8% 跌超5%）。深跌+缩量+资金进场 60日 +33.44% 但 n=22 太小，只能当观察口径。

### 中长线关键位置引擎 S1 落地（用户选「完整改造」）
- console.html 新增 `STAGE_VERSION='S1'` + `weekStage(code)`（纯展示、零额外请求）：四维度 = 位置 pos250（近一年分位）/ 结构（周线 MA5 vs MA20）/ 量能（4周÷26周周均量）/ 资金（近20交易日主力净额 ÷ 流通市值）。
- 只发三类关键提示：**主力建仓区**（净流入+低位+周线多头）/ **派发完毕**（高位+60日大涨+缩量或资金转负，⚠️2025 反向只作提示）/ **下跌未完·勿抄**（深跌+缩量+无资金）；其余 = **趋势运行中·无需操作**（不给信号）。
- 噪音治理：短期 F4「综合」行改为**只在结论变化时**才显示（`sa_ml_f4_v1`），中长线变化才打「新出现」（`sa_ml_state_v1`）—— localStorage 持久化比对。
- 数据层三处小改：①行情 ulist fields 加 **f21（流通市值）**，map 存 mv；②`chipLiveFrom` 返回 card 带 `flowDaily`（东财 120 日序列）+ `floatShares`；③`chipEnsure` 把已抓的 500 根日K 存 `G.dayBars[code]`（引擎复用，零额外请求）。
- 测试：position-dom-test 547 全绿（新增 S1 八项 + 综合行"仅变化时提示"断言）；accept-online 新增 4 条 S1 断言。全量回归通过。
- 真实数据冒烟（bt-data 腾讯 qfq）：600058/600703/601127/603026 → 下跌未完（与近期 −24%~−40% 跌幅一致），其余趋势运行中。烟测里 mainRatio 为—是因为烟测没给 mv（生产由 f21 提供）。
- ⚠️ 已知依赖：引擎需要 mv（行情 f21）才能算资金维度；mv 缺失时资金维度为 NaN → 不出建仓区结论（诚实降级，不硬凑）。

### 三安光电（600703）S1 一年单只回放（用户要求「测试最近1年按S1的信号正确率及收益」）
- 新增 **`_tests/s1-single.js`**（可复用：`node _tests/s1-single.js <代码>`）：jsdom 装 `_preview/stock-picker-preview.html`，逐日灌 bars[0..t] + 日期≤t 的资金流 + `mv=floatShares×close` 进 `w.G`，调**真代码** `w.weekStage()` 逐日取态 → 算 20/40/60 日前瞻 + MAE/MFE + 状态时间线。⚠️ 跑前必须先 `node _tests/build-preview.js`。输出 `_tests/s1-single-<code>.md` / `_cache/s1-single-<code>.json`。
- 600703 结果（2025-09-17~2026-09-18，244 交易日）：状态分布 趋势 228 天(93.4%) / 建仓区 7 / 下跌未完 8 / 派发完毕 1。买入持有 −14.77%。
- **建仓区（3-26、6-12 两段）**：按事件 60 日命中 **1/2（50%）**，收益 +67.85% / −6.25%，累计 **+57.36%**；⚠️ 但 3-26 买入后 5 日内先跌 −12.24% 才起飞（信号偏早 2 周）。按天统计 +60.56%/胜率 85.7% 是**重叠窗口虚增**，不可信。
- **派发完毕（3-02）**：后 20 日 −25.15%、60 日 −3.81%，方向对（躲对）。
- **下跌未完（8-28 起 3 段）**：均未满 60 日，到 9-18 分别 −1.07%/+3.10%/+1.09%；且**状态闪烁**——同一关键态被「趋势运行中」打断 2 次（4月...），说明关键位置态也不总稳定驻留。
- **最大失败（必须讲）**：4-02(11.40) → 6-24(21.81) 的 **+91.32% 主升**，55 天里 53 天显示「无需操作」，且 6-24 见顶后到 9-18 **−40.49%** 全程没有「派发完毕」——**S1 抓不住短命暴涨、也不给顶部预警**，它的强项只在低位+资金进场的建仓区。
- 局限如实写进报告：单只单年样本比 console 的 17 只×5440 点小 2 个数量级；资金用新浪 r0 代理（与东财大单+超大单方向一致率 ~75%）；mv 按当日收盘折算 vs 线上取当日实时；无未来函数（已逐日隔离验证）。

### 晋控电力（000767）S1 一年回放 + 脚本换股暴露的 2 个自伤 bug
- ⚠️ **换股票时暴露脚本硬编码**：① 报告标题写死「三安光电（600703）」；② 第六节「区间最低收盘 11.40（2026-04-02）」是三安的价，换股后算出「6.14 → −46% 的主升段」这种自相矛盾的句子。**已改为全部动态计算**（峰值前最低价、上涨段交易日、关键态天数、局限里的段数）。另修：rows 漏存 `wma5/wma20` → 第四节误报「周线结构数据不足」（引擎其实算得出，是报告脚本的锅）；新增①第四节「周线结构 + 建仓区四条件逐条 ✅/❌ 体检」②三之二「位置体检：收盘价距近一年最低价多远」。
- 000767 结果（244 交易日）：趋势 233 天(95.5%) / 建仓区 5 / 下跌未完 6 / **派发完毕 0**。买入持有 **+9.87%**（3.14→3.45）。
- **建仓区（6-23 一段，5 天）**：按事件 60 日命中 **0/1**，收益 **−17.45%**（最大浮亏 −23.35%）。按建仓区操作 = −17.45%，**跑输买入持有 27.3 个百分点**。
- **下跌未完（8-17/8-20/8-24/8-28 四段，状态闪烁）**：均未满 60 日，至今 −5.48%/+1.17%/+0.88%/0.00%。
- **漏掉**：2025-12-18(2.79) → 2026-06-01(6.14) 的 **+120.07%**，106 个交易日 **100% 显示「无需操作」**；6-01 见顶后到 9-18 **−43.81%**，「派发完毕」0 天。
- ★ **新发现的系统性弱点（两个案例互证，值得反馈进引擎）**：建仓区只看 pos250<0.4，但 **pos250 会被「一年内暴涨后的高点」撑大** —— 000767 在 6-23 触发时收盘 4.24 已是近一年最低 2.76 的 **+53.6%**（半山腰），pos250 却只有 39% 判成「低位」→ 假建仓区。对照 600703 两次建仓区分别在一年低点上方 +26.4% / +22.9%，才是真低位。**候选修法：建仓区增加「距近一年最低价 < 30%」闸门（或把 pos250 换成 现价/一年低点 口径）**，但必须先在 17 只×5440 点上回测，不能直接上线。
- ⚠️ 另一结构性观察：**「派发完毕」在两只股上几乎不触发（0 天 / 1 天）** → S1 的「顶部预警」那一半实际上近似失效，目前起作用的只有建仓区 + 下跌未完。当前 000767 = 趋势运行中，四条件里资金/位置/60日涨跌都 ✅，**唯独周MA5(3.46) < 周MA20(4.03) 卡住** → 按设计不喊建仓（解释得通，且方向上是保守的）。

### 用户「修」→ 建仓区两道位置闸门 + 周级支撑压力 + 阶段顶部辅助（2026-09-20 上午）
- 用户指令：「修」→ 先把候选闸门放回测，再改代码。
- ⚠️ **回测口径纠正**：`_tests/f4-selfcheck.js`/`s1-gate-test.js` 原来用**收盘价**算 pos250，而线上 `weekStage` 用**盘中高低价**（区间更宽→分位更低）。同一规则线上真口径是 **n=124 / +14.69% / 跌超5% 16.1%**，不是 console 上写的 n=112/+14.80%。已把 console 数字全部改对。**新增脚本 `_tests/s1-gate-test.js` 同时算 pos250(收盘) 与 pos250i(盘中)，以后一律用 pos250i。**
- **闸门 A「距一年最低价 < 30%」**（`ML_LOW_GATE`）：n 124→117、+14.69%→+15.95%、跌超5% 16.1%→13.7%；被剔除的 7 个样本 60日 −6.35%/跌超5% 57.1%（确实更差）。更紧的 20% 版本样本内更好(+18.42%)但会误杀 600703-03-26(+67.85%)，故取 30%。
- **闸门 B「距近 20 周最低价 < 15%」**（`ML_SUP_GATE`，用户要的「同周期支撑位」）：n 117→**69**、60日 **+25.23%**、跌超5% **2.9%**、胜率 97.1%、**3/3 时段正（+16.46/+25.52/+51.36）**。★ 关键结构性事实：**20 周窗口 ⊂ 一年窗口 ⇒ supDist ≤ premLow 恒成立**，所以 B 比 A 严格、B 才是真正起作用那道。逐只：9 只有信号的股票 7 只为正；把 002594(13 个负样本)、000767(4 个负样本) 全筛掉；600703 保留 6 个（含 03-26），该股从 +60.56% 升到 **+71.70%**。
- **周级压力辅助**（近 20 周最高价）：派发完毕若同时「已从周级压力回落 >10%」→ 60日 **−12.30%**、74.6% 跌超5%（n=63），但 2025H2 仍 +7.50% 反向 → **只写进文案，不改触发条件**。
- **阶段顶部辅助（用户原规则）实测**：MA5>MA10>MA20>MA30 且连续≥2日开&收站上当日 MA5 → **单独看几乎无预测力**（60日 +1.75% vs 基准 +4.45%、跌超5% 39.5%，n=430）；对照组「非多头排列+连续≥2日」60日 +3.07% 反而更好 → **证明「须多头排列」这个前提是对的**。只有叠加「偏离 MA5 >5% 且 pos250>0.65」才有效：60日 **−8.02%**、66.2% 跌超5%（n=74），但效力集中在 2026H1(−21.75%)，2025 两时段≈0。→ 该形态**控制台早就有了**（首页「连续 N 天站上 5 日线」= SignalCore.ma5Streak，是 R3 推送口径），所以**没有重复加信号**，只把它实测的力度与「什么条件下才真有效」补进中长线块。
- 引擎改动（console.html）：加 `ML_LOW_GATE=0.30` / `ML_SUP_GATE=0.15` 两个具名常量；周聚合补 `h/l`（周内高低）；新增 `supDist/resDist/wHi20/wLo20` 与日线 `maAlign/streak5/overMa5/topWarn/topStrong` 指标；建仓区加两道闸门；派发完毕文案按 resDist 分级；trending 分支在 topStrong 时改用「短期已偏热」文案；详情页新增「距周级支撑/距周级压力/短期热度」三格 + 顶部辅助说明；口径注释与回测数字全部更新。
- 测试：position-dom-test **556 通过 / 0 失败**（新增 ②e~②j 六条：闸门 B 专项、supDist/resDist 暴露、阶段顶部置位/不置位；②b 期望改 +25.23%）；accept-online 新增 3 条（ML_SUP_GATE/距周级支撑压力/topWarn、−5.18% 更正）；**全量离线回归全部通过**。
- **改后全量重验**（`_tests/s1-all-check.js` → `s1-after-report.md`，17 只逐日回放）：一年内只有 6 只出过建仓区（600703/600733/603162/002349/000155/002577），其余 11 只全年无建仓区信号；000767 误报 **5 天 → 0 天**；600703 由 7 天/2 事件变为 **6 天/1 事件**（只剩 2026-03-26，+67.85%）。当前 2026-09-18 状态：**600703 与 600058 = 下跌未完**，其余 15 只趋势运行中；**17 只目前无一满足阶段顶部条件**（短期热度全部「正常」）。
- ⚠️ 阈值选择存在**多重检验风险**（在 6~7 个候选里挑的最优），报告里已如实标注；20%/15% 与 30% 的差距不宜当真实差异。

### ★ 用户批评「一年都不触发信号 = 死拿」→ 漏斗诊断 + 重构候选回测（2026-09-20 午前·下半场）
用户原话：「我是中长期波段，不能一年都不触发信号，那还监控什么，死拿就行了。我要的是底部抄底，高位卖出。不要为了判断而判断。思考下怎么来完成我的目的，找问题所在。」

- **诊断脚本 `_tests/s1-funnel.js`**（→ `s1-funnel-report.md`）：现有买点是 AND 长链，最后一步「资金>0.4%」把样本从 192 砍到 69（**每只每年 0.87 次**、9/17 只有信号）；逐只表证明 **「位置低(pos<0.4)」与「周线多头」天然互斥**（300132：295 天周线多头 vs 18 天位置低；002471：245 vs 12）——两条件同时成立只剩"刚见底反转"那几周。**这就是全年沉默的根因。**
- **卖出侧三个直觉信号全部反向**（高位+资金转负 +2.96%／高位+缩量 +5.28%／高位+周线转空 +5.16%，60日后竟然是涨的）→ 唯一有效的是「高位+短期过热(偏离MA5)」。
- **重构候选回测 `_tests/s1-redesign.js` + `s1-redesign2.js`**（→ 两份 report）。关键结论：
  - 「周线仍空」不是必要条件、**反而是拖累**（资金+位置：不管周线 +9.72% → 加「周线空」降到 +8.21% → 加「周线多」升到 +14.69%）。所以**左侧潜伏不需要"周线空"，而是"去掉周线多头这个硬门槛"**。
  - ★ **最优买点 = 位置<0.4 ＋ 资金>0.4% ＋ 距20周支撑<15%（不带周线条件）**：n=244、20日 +2.28%、**60日 +13.01%**、跌超5% **13.5%**、**每只每年 2.17 次**、覆盖 **15/17**、**3/3 时段全正（+9.30/+17.51/+11.28）** —— 频次与质量同时达标。放松到 0.2% 资金 → 2.96 次/年但 60日 降到 +7.13%（2026H1 甚至 −1.36%）。收紧到 <0.35 → 1.87 次/年 +13.13%。
  - 单因子贡献：位置<0.4（+3.86%）、资金>0.4%（+3.71%）、周线多头（+5.65%）、距20周支撑<15%（+4.13%）—— 单看都平庸，**组合后 +13%**，说明是"低位＋资金真进＋离支撑近"三因子共振。
  - ★ **最优卖点 = 位置>0.65 ＋ 偏离MA5>5%**：n=226、60日 −4.02%、跌超5% **56.2%**（基准 33.9%）、**每只每年 5.31 次**、覆盖 15/17；⚠️ 但 **2025 两段反向（+5.28%/+5.26%），只有 2026H1 显著（−17.55%）**。加强版（+多头排列+连续≥2日，即用户原规则）：60日 **−8.02%**、跌超5% 66.2%，但每只每年仅 **1.87 次**、13/17。
  - 见顶破位（曾高位 high60>0.8 + 现周线空）：60日 −4.01%、每只每年 1.00 次；+距压力<−15% → −5.31%/1.22 次 —— 频次太低且同样只 2026H1 有效。
  - 逐只总频次（候选规则集）：**平均每只每年合计 11.0 次**（建仓区 0.87 ＋ 低位关注 2.96 ＋ 减仓-强 1.87 ＋ 减仓-标准 5.31）；最少的雷柏科技 3 次/年，仍远好于现状的 0。
- **给用户的方案（尚未实施，等确认）**：①买点拆两级 —— 新「低位关注」（位置<0.4＋资金>0.4%＋距20周支撑<15%，不带周线门槛）打底频次，满足周线多头的升级为「建仓区」保持 +25.23% 强度；②卖点拆两级 —— 「减仓提醒」（位置>0.65＋偏离MA5>5%）＋加强版；③如实告知：卖出侧**没有可靠的"中期见顶清仓"信号**（数据反向），只能做短期过热提醒，且效力随市场切换。
- 报告：`_tests/s1-redesign-report.md`、`s1-redesign2-report.md`、`s1-funnel-report.md`。

### ★ 用户追问「这11次信号准确度和收益怎么样」→ 策略级回测 `_tests/s1-eval.js`（→ `s1-eval-report.md`）
- ⚠️ **v1 有造假 bug 已修**：只统计「已平仓」交易 → 买了不涨、卖点永不触发、被迫拿到结束的**浮亏持仓被漏掉**，得出假的「每笔胜率 100%」。v2 把未平仓按最后收盘记账、并标注 `why`（sell/stop/hold）。
- **信号准确度（60日口径，样本 17 只 × 390 交易日 ≈ 1.6 年，基准 +4.45%/胜率 55.3%/跌超5% 33.9%）**：
  | 信号 | 频次 | 60日 | 胜率 | 跌超5% |
  |---|---|---|---|---|
  | 买·建仓区 | 0.77 段/年 | **+25.23%** | **97.1%** | **2.9%** |
  | 买·低位关注（含建仓区）| 3.09 段/年 | +13.01% | 77.0% | 13.5% |
  | 买·**新增部分**（低位关注 − 建仓区）| **2.43 段/年** | **+8.19%** | 69.1% | 17.7% |
  | 卖·减仓提醒 | 4.97 段/年 | −4.02% | 64.6% 后续下跌 | 56.2% |
  | 卖·加强过热 | 1.73 段/年 | **−8.02%** | **75.7% 后续下跌** | 66.2% |
- ★ **位置验证通过**：买点触发时平均位置分位 **22.0%**、卖点 **88.4%**（全样本 49.6%）→ 确实"低位买、高位卖"。
- ★★ **频次是有代价的（必须承认）**：新增的 2.43 段/年 质量明显低于建仓区（+8.19% vs +25.23%、胜率 69.1% vs 97.1%、跌超5% 17.7% vs 2.9%），但仍显著优于基准（+4.45%/55.3%/33.9%）。**不能既高频又高质量。**
- ★★ **策略层面打不过死拿**：死拿（同窗口）+25.08%/组合回撤 −28.44%；S2 全进全出 +13.57%/−7.66%；S3 +20.07%/−7.24%。**这段样本是牛市（17 只平均 +25%，含海通发展+146%、中超控股+131%，基准本身偏高且有选股偏差），全进全出会空仓 65% 时间 → 数学上必然跑输。**
- ★★★ **发现真实设计漏洞**：卖点只在「高位过热」触发 ⇒ **买错的仓位永远不会触发卖点**。S2 的 **8 笔亏损单全部是 `hold`**（买了不涨、卖点不响、被迫拿到结束），最惨 −42.53%。**原方案缺止损。** 补 −10% 止损后：收益 13.57→**14.32%**、回撤 7.66→**7.32%**，但胜率 66.7%→51.4%（14 笔止损）。
- **仓位管理 > 全进全出**（底仓常持＋信号加减仓，含 −10% 止损）：底仓 70%/买点→100%/卖点→30% → 逐只均 **+18.14%**、组合回撤 −13.15%；底仓 50%/卖点→20% → +16.37%/−10.95%；底仓 50%/卖点→0% → +13.80%/−7.22%。
- **风险调整后（收益÷回撤）**：S1 建仓区 **2.39**、S3 **2.77**、S2 1.77、仓位70% 1.38、**死拿 0.88** → 信号系统的价值在**控回撤**，不在提收益。
- 结论/给用户的定位：**别把它当"全进全出的择时系统"**（牛市必输死拿），正确用法 = **底仓常持 ＋ 买点加仓 ＋ 卖点减仓 ＋ 硬止损**；且必须补止损，否则买错的仓位没有出口。

### ★ 真实个案压力测试：梦网科技(002123) 2025-01-17/01-27/02-06/02-07（用户指定日期）
- 新脚本：`_tests/probe-mw.js`（探数据）、`_tests/mw-fetch.js`（取数→`_cache/mw-data.json`）、`_tests/mw-s1.js`（回看→`mw-s1-report.md`）。数据：腾讯 qfq 619 根 2023-06-01~2025-12-31；新浪 r0 资金流回溯到 2021；603 流通股本 7.21 亿股。
- **⚠️ 发现口径不一致（影响此前所有准确度数字的解释）**：console 的 `weekStage` 周聚合是 `for(k=0;k<=i;k++)` → **含当周（可能是未走完的残周）**；而我的回测（s1-eval/s1-redesign/s1-funnel）用的是**只取完整周**。两者在周一~周四会分歧（残周的 h/l/v 被计入 → supDist 偏小、volRatio 偏移）。**周五两者一致。** 待评估是否统一。
- **行情事实**：一年最低 5.43（2024-08-28）→ 2024-09 起涨 → 2024-12~2025-01 在 10~13 震荡 → 2025-01-17 **涨停开盘后炸板**收 11.76（放量 149.9 万手）→ 01-27 **缩量回踩** 10.66（28.8 万手）→ 节后 **02-05~02-14 连续 8 个涨停** → 02-17 见顶 25.15 → 4 月跌回 13~14。
- **四个日期的系统输出（旧/新系统口径一致）**：01-17（位置 71%、资金 +9.5%）**无信号**、01-27（68%、+5.1%）**无信号**；02-06（98%、+11.3%）与 02-07（100%、+17.7%）**加强过热=减仓**（旧系统同期喊"派发完毕"）。事后：01-17 后 20 日 **+46.6%**、01-27 **+76.6%**、02-06 **+49.8%**、02-07 **+25.4%**。
- ★★ **结论：这四个日期系统全错**——该买的时候不喊（位置分位 68~71%，超"<40%"门槛），喊减仓的时候还在连板途中。
- ★ **高光案例（新系统真实价值）**：2024-09-03~09-11（6.00~6.21，位置分位 5~7%、资金 +0.4~0.9%、距20周支撑 10~14%）→ **「低位关注」买点**，后 20 日 **+55% ~ +83%**；**旧系统同期说"趋势运行中·无需操作"**（卡在周线未转多）→ 这就是补"左侧潜伏"的意义。
- ★★★ **硬伤 1：单只股票在下跌通道里，买点会反复触发反复亏**。2024-06-06~07-09 触发 **11 次买点，后 20 日全为负**（−1.71% ~ −18.42%）。按事件分 3 段：06-06 段 −7.23%、07-02 段 −10.21%、09-03 段 +55.04% → **事件命中 1/3**；但 17 天平均值 **+17.12%**（小亏多次＋大赚几次，**不是高胜率**）。⇒ 回测那个"胜率 77%"是 17 只平均＋牛市样本，**不能移植到单只下跌股**。
- ★★ **硬伤 2：连板途中「减仓提醒」会连续误报**。2025-02-05~02-14 连报 8 天（连板必然"偏离MA5 巨大"），而 02-05 收盘 11.73 距顶部 25.15 **还有 +114%**。这正是回测中"卖点 2025H1 反向(+5.28%)"的具体成因。⚠️ 但 60 日口径看：02-07 卖出价 14.19 vs 持有 60 日 14.95 → **少赚 5%，却躲过 −22.4% 回撤** ⇒ 它是"别追加＋分批兑现"，不是清仓信号。
- ★ **真底部也没抓到**：2024-08-28（5.43）资金仅 **+0.18%**（<0.4% 门槛）→ 无信号；旧系统还喊"下跌未完·勿抄"。事后 60 日 **+90.34%**。只差 4 个交易日、6.0 买入 vs 5.59 仅高 7%。
- ★ **盲区（值得补的规则）**：2025-01-27 是"**缩量回踩（volRatio 0.62、偏离MA5 −3.1%）＋ 周线多头 ＋ 资金净流入 +5.1%**"的**二次买点**形态，位置分位 68% 因此被判"不低"而漏掉——但它是主升起跳点（后 20 日 +76.6%）。F4 里已有 `pullback` 回踩概念（回踩·建仓 20日 +4.76%、4/4 时段正）可借鉴。**需回测后再决定是否加。**

### ⚠️⚠️ 用户质问「你用的什么跑的，是本次修改后的系统吗」→ 自查发现两个实质问题（2026-09-20 午）
- **问题 1：`mw-s1.js` 不是真代码。** 它是我自己按回测口径**重写**了一遍规则；而「低位关注 / 减仓提醒」**根本不在 console 里**（只是回测候选，用户尚未批准实施）。我上一轮把候选规则说成"新系统 → 减仓提醒"，**措辞不严谨，等于把候选当成了系统**。同样，我说的"系统高光：2024-09 低位关注买点"其实是**候选规则**的高光——**真代码在 2024-09-03~09-11 全程输出「趋势运行中·无需操作」**（仍卡周线多头），它没有抓到那波。
- **问题 2：口径与 console 不一致。** 已用 `_tests/mw-real.js`（jsdom 装预览 → 调**真实 `w.weekStage()`**，与 s1-single.js 同法）核对：**console 的周聚合是「含当周残周」（= 我的口径B），而我此前所有回测（s1-eval / s1-funnel / s1-redesign / s1-gate-test / s1-sr-test）用的是「只取完整周」（口径A）**。⇒ **那些准确度数字（+25.23% / +13.01% / +7.13% / −4.02% 等）与线上真口径有偏差，需用口径B 重跑核对。** 已确认 191 个扫描日里我的复现与真代码有 **57 天（30%）不一致**，主因三条：①候选规则 vs console 的固有差异（console 要周线多头）②我复现漏建"下跌未完"态 ③**资金口径**（见下）。
- **资金口径细节**：真代码取 `flowDaily` 的**末 20 条**；我取"末 20 根K线对应日"。本案差近一倍（01-16：真代码 +4.55% vs 我 +9.53%）。
- ★★★ **重大数据发现：梦网科技 2025-01-02 ~ 2025-01-15 停牌 10 个交易日**。腾讯 qfq 与**腾讯不复权**日K 都缺这 10 天（交叉证实，非数据缺失）；12-31 收 10.81 → 01-16 复牌收 11.89（+9.99% 涨停）→ 01-17 涨停开盘 13.08 炸板收 11.76。**新浪 moneyflow 对停牌日返回 0 值**，而线上东财 fflow 只给交易日 —— 所以真代码在我喂的新浪数据上把 10 个 0 算进了 20 日窗口（偏低估），**我复现的 +9.53% 反而更接近线上东财口径**。⇒ 教训：**喂给回放的资金源若含停牌日 0 值，会污染窗口**；且 S1 没有"停复牌"概念，复牌跳空会污染均线/周线。
- **真代码四日期输出（更正版）**：01-17 **趋势运行中**（原文"中长线按原计划持有即可，无需操作"）后20日 +46.6%；01-27 **趋势运行中** 后20日 +76.6%；02-06 **派发完毕** 后20日 +49.8%；02-07 **派发完毕** 后20日 +25.4%。⇒ 结论方向不变（四天都不给买点、02-06/07 喊卖），但**02-06/07 的答案要说"派发完毕"，不是我上次说的"减仓提醒"**。
- 新脚本：`_tests/mw-real.js`（→`mw-real-report.md`，真代码回放＋与复现逐日对比）、`_tests/mw-flowdiag.js`（资金口径诊断）、`_tests/mw-halt.js`（停牌交叉验证）。

### ★★★ 用户指导「不要对单只股票过度拟合、要在单日精确性和波段定性上考虑」→ 全 17 只波段级评估（2026-09-20 午）
用户原话：「不要对一个股票去过度拟合，要整体去思考去判断，你改后其他股票有没有影响，你给的可以是定性的，出货也不是一天能完成的，底部也是慢慢走出来的，你要在单日精确性和波段定性上考虑。」
- 新脚本 `_tests/s1-zone.js`（→ `s1-zone-report.md`）：**用真代码 `weekStage`** 逐日取指标（保证口径=console 含当周）→ 把信号当**区间**（连续满足，允许 ≤5 交易日抖动合并）→ **区间级**评估（一段算一次）。区间合并容差 GAP=5。
- ★★ **推翻我上一轮的核心建议：「周线多头」不是拖累，它是撑住整条买点质量的那一根**。区间级 + console 真口径下：现行建仓区 **12 段/覆盖 7-17 只/0.45 段每年、区间起点买入 60日 +24.6%（正收益率 90%）**；**去掉周线多头 → 55 段/15 只/2.08 段每年、但只剩 +5.8%（正收益率 65%）** —— 频次×4.6、质量几乎归零（基准 +4.4%）。上一轮我说"+13.01%（口径A/日级）"是**假象**。
- ★★★ **消融实验（从现行 6 条出发，每次只去掉一条）** —— 「区间/覆盖/每只每年/起点60日/正收益率/建仓期浮亏」：
  | 规则 | 区间 | 覆盖 | 段/年 | 起点60日 | 正收益率 | 建仓浮亏 |
  |---|---|---|---|---|---|---|
  | 基线（全 6 条） | 12 | 7/17 | 0.45 | **+24.6%** | **90%** | −2.9% |
  | 去掉 60日涨<15% | 12 | 7/17 | 0.45 | +24.6% | 90% | −2.9% ← **完全冗余，可删** |
  | 去掉 距一年低<30% | 14 | 8/17 | 0.53 | +21.0% | 92% | −2.9% |
  | 去掉 距20周支撑<15% | 20 | 9/17 | 0.76 | +18.4% | 81% | −3.3% |
  | 去掉 位置<40% | 18 | 10/17 | 0.68 | +13.3% | 63% | −3.9% |
  | 去掉 资金>0.4% | 22 | 12/17 | 0.83 | +11.4% | 68% | −4.0% |
  | 只留 周线多头+位置<40% | 33 | 14/17 | 1.25 | +10.0% | 71% | −4.9% |
  | **去掉 周线多头** | 55 | 15/17 | 2.08 | **+5.8%** | 65% | −4.2% |
  | 只留 位置+资金+支撑<15% | 61 | 16/17 | 2.30 | +4.0% | 57% | −4.5% |
  | 只留 周线多头+资金>0.4% | 75 | 17/17 | 2.83 | +6.8% | 57% | **−11.0%** |
  ⇒ **频次与质量是一条连续取舍曲线，没有免费午餐**：想"多喊"就必然掉到基准附近。四条位置/资金条件的各自分量：资金 ~13pp、位置 ~11pp、距20周支撑 ~6pp、距一年低 ~3.6pp、60日涨 **0**（冗余）。
- ★★ **卖点：现行「派发完毕」躲对率只有 50%（等于抛硬币）**。全 17 只区间级：现行 37 段/躲对率 50%/后悔值 +26.7% → 候选(位置>65%+偏离MA5>5%) 95 段/63%/+24.8% → **候选加强(＋多头排列＋连续≥2日站上MA5，即用户提的那条) 45 段/躲对率 76%/躲对幅度 +17.1%/后悔值 +22.4%**。**三项全面优于现行 ⇒ 这条应当采纳。**
- ★★ **「改动对其他股票的影响」**：买点放宽覆盖 7→16 只，但新增覆盖的 9 只里 **赛力斯 −29.9%、中超 −15.8%、雷柏 −10.4%、北汽 −7.9%、五矿 −4.9% 全是负贡献**，受益只有极少数（百花 +69.3%、三安 +67.9%、晋控 +30.5%）；净效果=拉回基准。**卖点改进是全 17 只一致受益，没有为谁特调。**
- **结论/给用户的定性设计**：①买点**不动**（只删冗余的「60日涨<15%」）；稀有是特性不是 bug——"真底就那么几次，喊不出来时闭嘴比乱喊好"。②卖点**改成加强版**。③输出形态从"单日指令"改为**波段定性**：底部区间平均持续约 8 个交易日、派发区间约 4 个交易日，"底部慢慢走出来"要看区间均价而非某一天的最低价。

### ★ S1 → S2 换版落地（用户选「全采纳」+「接受稀有但要说清代价」）
- console.html 五处改动：①删掉买点冗余条件 `ret60<0.15`；②卖点由 `distribute-done`（派发完毕）换成 **`sell-overheat`（高位过热·减仓）** = 多头排列 + 连续≥2日开&收站上当日MA5 + 偏离MA5>5% + pos250>0.65，新增具名常量 `ML_SELL_MA5=0.05 / ML_SELL_POS=0.65 / ML_SELL_STREAK=2`；③**波段定性** `mlRunDays()` 按日K 数本态持续交易日（零额外请求），`mlChanged` 改为**只在状态变化时刷新起始日 d**（否则天数永远=1），详情页「中长线 → X（第 N 天）」+ 区间均价说明，列表行「· 第 N 天」；④买点文案说清代价（0.45 段/年、建仓期平均还要浮亏 −2.9%）；⑤高位但未过热时 trending 分支明说「旧规则躲对率仅 50% 已停用」，避免"高位为什么不提示"的困惑。`STAGE_VERSION` S1→S2。
- **真代码重验（s1-zone.js / s1-all-check.js）**：买点删冗余后**逐项零变化**（12 段/7-17 只/+24.6%/浮亏 −2.9%）✓；卖点 **37 段 / 躲对率 50% / 躲对幅度 +8.5% / 后悔 +26.7% → 45 段 / 76% / +17.1% / +22.4%**，每只每年 1.40→1.70 段。
- ★ **高光（新卖点抓到了旧规则漏掉的顶）**：**三安光电 2026-06-24~06-25、晋控电力 2026-06-02 现在都出「高位过热·减仓」** —— 这两处正是旧规则 0 天、其后分别跌 −40.49% / −43.81% 的顶。用户 5 只自选现在全都有卖点（石大胜华 7 段 / 西藏矿业 3 段 / 川能动力 4 段 / 晋控 2 段 / 三安 2 段）；建仓区一年内只有 600703/600733/603162/002349/000155/002577 出过。
- 测试：position-dom-test **572 通过 / 0 失败**（新增 ②b-2、③③b③c③d③e、⑥b~⑥f、⑨⑨b⑨c⑨d⑨e⑨f⑨g）；accept-online 的 S1 断言全部改写为 S2（含**负断言**：`ret60 < 0.15` 与 `distribute-done` 必须不存在）；**全量离线回归 16 项全绿**。
- ⚠️ **测试夹具坑（重要）**：`renderDash` 会走 `chipEnsure` 重新取数 → **注入的 chipMap 被覆盖**，所以「首页行显示第 N 天」在 DOM 层验不了（weekStage 会掉回 trending）。改用**渲染代码静态断言**（`' · 第 ' + rd + ' 天'`）。以后测列表层渲染先想这个。
- 未发布（用户未说发布）。
- **缺口盘点（已告知用户，等其拍板）**：①买点覆盖 7/17 → 10 只一年不响；②**中期见顶仍无解**（只有短期过热），且 2025 两时段反向；③**无止损/失效条件**（策略回测：8 笔亏损单全是 hold，最惨 −42.53%；补 −10% 止损后收益 13.57→14.32%、回撤 7.66→7.32%）；④无仓位建议；⑤因子只有价格/资金衍生量 —— **筹码分布（ChipCore）与大盘风格驾驶舱（style.js 七档+冰点退潮）已建好但未接入 weekStage**，这是补"顶部预警"最省的路。

### ★ S2 收口（2026-09-20）：止损 + 筹码 + 大盘环境三件一起落地
- 用户三个拍板：①买错仓位**统一 −10% 止损**；②补顶部缺口走「**接已建好未接的模块**」；③系统定位 = **持仓管理助手**（不做架构改造）。
- ① **统一 −10% 止损**（`var ML_STOP = 0.10` / `mlStopLoss` / `mlStopHtml`）：有持仓成本 = 成本×0.9；没成本 = 本态起始日收盘×0.9（界面必须标「近似」）；两者都无 = 返回 null **不编数字**。与状态**解耦**（只要有成本就展示，不管是不是关键位置）。代价已实测：收益 13.57→14.32%、回撤 7.66→7.32%，但胜率 66.7%→51.4%（14 次止损）—— 止损是给「买错了」留出口，不是提高收益。
- ② **筹码接入（只分级、不触发）**：`weekStage` 的 `m` 多带 `profitRatio/conc/mainCost/mcDev`（读 `p.chips`，零额外请求）；卖点 `m.chipGrade` = thick（mcDev≥0.15，0.15 是 chip-core 已有 bigMove 参数，非本次新挑）/ thin / none。依据 `_tests/s1-chipadd.js`（17 只×6460 天、**中位切分不扫阈值**）：**买点零增量**（建仓区本身已 +25.5%）；**卖点有增量**（现价/主力成本 ≥15% 那半后 60 日 −11.3% vs <15% 的 −4.8%，方向符机制）—— 但样本仅 74 天/13 只，**不够当闸门，只写进结论**。
- ③ **大盘环境接入（只标注、不触发）**：`mlMarketCtx()` 纯读已加载的 `STYLE.data` + `FNG`（**零额外请求**）；`mlFngZone()`（≥70 贪婪 / ≤30 恐慌 / 其余中性）；`mlMarketEnsure()` **每会话只懒加载一次且自己不重画**（靠行情轮询自然刷新 → 彻底避开 renderDash 无限循环，见第 128 行那个坑的同类风险）；详情页出「大盘风格 / 情绪周期 / 恐贪指数」，恐贪 ≤30 且 `ws.key==='sell-overheat'` 时补一句「减仓可更果断」。
  - 依据 `_tests/s1-market.js` + `_tests/s1-market-report.md`（新增 `_tests/_cache/index-fetch.js` → `bt-index.json`，3 指数×640 根）：**风格七档存档 2026-09-19 才有、只有 1 天 → 完全无法回测**（只能展示，等它自己攒历史）；**恐贪可回测**（fng-history.json 750 天，最早 2023-08-15）：卖点恐贪低位 −11.5% vs 高位 −5.2%、上证跌破 MA20 −15.5% vs 上方 −6.3%（方向符机制，但 n=14 太小）；**买点方向相反**（弱市 +31.0% vs 强市 +22.5%）→ 两者都不能当闸门。
- 测试：position-dom-test **615 通过 / 0 失败**（新增 S2⑫~⑫o 大盘环境 16 条 + S2⑬~⑬f 加粗渲染 6 条）；accept-online 补 3 条大盘/加粗断言（**须发布后才能跑**）；**全量离线回归 16 项全绿**。
- ★ **顺手修掉一个真渲染缺陷**：结论文案里的 `**强调**` 一直是**字面显示**（界面上真能看到星号）—— 全 console 非注释处共 9 处。修法：新增 `mdB(s)`（`**x**` → `<b>x</b>`），raw 串直接 `mdB(str)`，需要转义的串用 **`mdB(esc(str))`（先转义再替换，避免注入）**；raw 处 4 条已直接改写成 `<b>`，`ws.advice` 走 `mdB(esc(ws.advice))`。**展示层改动，算法/阈值一字未动。**
- 可视快照 `_tests/s2-snapshot.js` → `_preview/s2-market-block.html`（构造演示数据，肉眼确认「中长线 + 筹码维度 + 大盘环境 + 恐慌力度提示 + 风险控制」五块的排版与降级路径）。⚠️ 坑：`mlMarketCtx` 优先级 **FNG(若已加载) > STYLE.data.fng**，快照里两边都要给低位值，否则 demo 引导流程把恐贪页数据 load 进来会盖掉。
- **仍未解（如实留给用户）**：①买点覆盖 7/17 → 10 只一年不响（稀有是特性，已写清代价）；②**中期见顶仍无硬信号**（只有短期「高位过热」，且 2025 两时段反向）；③无止盈/换仓逻辑（只有 −10% 止损兜底）；④无仓位管理建议（除了止损，没说该买多少）。
- 未发布（用户未说发布）。

### ★★ 第 4 条「仓位纪律」：做了又降级 + 首次发布上线（2026-09-20 下午）
- **先做全量版，被用户砍掉**：`mlPositionHtml` 一开始做了「状态 → 目标仓位（满仓/30%/底仓）」+ 5 档账户对照表（收益/回撤/收益÷回撤）+ 逐只 4 步操作 + 等权参考。用户回：**「我不是很关心仓位建议，你可以降级这块。仓位我自己会控制，除非是必须与你的系统适配。」**
- **教训（重要）：用户问「距目标还差什么」时我列的缺口，是「我看到的缺口」，不等于「他要的缺口」。仓位属于用户自己的决策领域 —— 应该先问「这条要不要」再动手，别默认补上。**
- **降级后的形态（只留系统必须说清的一条）**：一行 `cd-note` —— 仓位**你自己控**；系统只要求「**别全进全出、留底仓常持**」，因为信号的价值在控回撤不在择时（全进全出 +13.57%/回撤 −7.66% vs 死拿 +25.08%/−28.44%）。同时**删净** `ML_BASE / mlPosTarget / mlPosN / mlPosPct` 与整张对照表，不留死代码；测试改为**负断言锁死**（详情页不得再出现「收益 ÷ 回撤」「底仓 70%」「等权参考」，源码不得再有 mlPosTarget/ML_BASE）。
- **首次发布上线**：`workbuddy_sites_deploy`（directory=`D:/mywork/stock-alert-console`、**entryHtml=`console.html`**（该目录有 console.html + design-preview.html 两个页面，必须指定）、`appName=A股监控控制台`、`domainPrefix=stock-alert-console`、`userAskedToPublish:true`、**`updateExistingApp:true`**）→ **shareLink 不变 `https://stock-alert-console.app.workbuddy.host/`**、`verified:true`。线上与本地 **md5 逐字节一致**。
- ⚠️ **accept-online 负断言又被注释误伤（第二次踩同一个坑）**：`on.indexOf('ret60 < 0.15') < 0` 被判失败 —— 因为注释里写着「已删掉原条件『ret60 < 0.15』」。修法：新增 `const onNoCmt`（去块注释 + 行注释），**所有「必须不存在」的断言一律判 onNoCmt**。
- 最终状态：全量离线回归 **16 项全绿**；position-dom-test **623 通过 / 0 失败**（⑭ 由 16 条改为 8 条降级锁定断言）；accept-online 192 通过 / 1 失败 → 该条已修。
- ⚠️ **降级后又改了内容 → 线上仍是带全量仓位块的旧版**。按发布规则（同意不跨轮），**需用户在本轮明确说发布才能同步**。

### 持仓详情页「去小字」+ 核心指标着色（同日稍晚，用户第 5 条）
- 用户原话：「如果对判定结果影响不大，可以不处理，大就更新。另外，持仓详情页的信息把特别小字的备注推理过程可以去掉，给我显示结论性和状态性的信息就行，重点核心指标的可以颜色突出下。」
- **(1) 线上同步判定 = 不处理**：逐行 diff 线上（`https://stock-alert-console.app.workbuddy.host/console.html`，325289B）vs 本地 → **只差 9 行，全部是仓位降级块**（一段注释 + 单行 `mlPositionHtml`）。纯展示、不影响判定 → 按用户规则**不同步**（线上仍是带全量仓位块的旧版）。
- **(2) 详情页精简**：`v3DetailHtml` 里 **`.cd-note` 小字块清零**（原 9 处整段推理全删）。新增 `.cd-stat` 状态条（good/bad/warn/mute，12px 可读）承接「必须留下的状态」：波段定性第 N 天 / 阶段顶部辅助 / 恐贪恐慌区力度 / 缺源告知 / 一行免责口径。
- **(3) 核心指标着色**：`cdCell(k,v,unit,tone)` 新增第 4 参；`.cd-v.good/.bad/.warn` = 加粗 + 蓝/橙/琥珀。**着色只挂在引擎自己的阈值上**（不凭感觉挑色）：一年位置 <40% 蓝 / >65% 橙（= 建仓区/派发区位置闸门）、周线结构 多头蓝/空头橙、近 4 周主力 ±蓝/橙、短期热度 琥珀/橙、现价高于主力成本 ≥15% 琥珀（= chipGrade 分级线）、多源共识 全票蓝/源不足琥珀、结构裁决 随 `psRuling.k`、恐贪极端值琥珀。
- **刻意不染色**：60 日涨跌、量比 VR、距周支撑/压力、周量能比、距一年低点、获利盘 —— 属「读事实」，没有公认好坏方向，染色反而误导。**仍沿用本卡避开红绿的约定**（蓝=可跟 / 橙=该躲 / 琥珀=留意 / 灰=中性），不引红绿。
- ⚠️ **结论块（`.cd-act`）里的「口径/代价」说明刻意保留**：`ws.advice` 与 `fusionDesc` 内含回测率/代价数字（如 0.45 次、再跌 2.9%、2025 两时段反向），是用户先前要求「说清代价/如实标注」时定下的，且 ②b/②b-2/③b/③c/⑤b/⑪b/⑪e 与 accept-online 都在**锁这些串**。所以本次只清「备注型小字」，不碰结论块 —— **两者不混为一谈**（要再精简结论块，须连这些断言一起改，用户确认后再动）。
- 测试同步：position-dom-test 新增 **S2⑮~⑮j**（cd-note 清零 + 6 段被删小字逐条负断言 + cd-act/cd-stat 保留 + cd-v 着色存在 + 60 日涨跌保持中性 + cdCell 4 参）；改写 ⑪g/⑪i/⑫b/⑫g/⑫g-2 与「融合规则 F4」那条（→ 改为「不再展开口径/回测数字」负断言）。accept-online 新增 1 条并改写 2 条（筹码/大盘口径撤下界面）。
- 结果：**全量离线回归 16 项全绿**；position-dom-test **638 通过 / 0 失败**。快照脚本 `_tests/s2-snapshot.js` 改造成「精简版」自检（着色格 13 / 状态条 9 / cd-note 0）。
- 未发布（用户本轮没说发布；按上一条规则，纯展示改动本就不必急）。

### 详情页精简版已发布上线（2026-09-20 下午，用户「全上就行」）
- 用户一句 **「全上就行」** = 明确发布 + 结论块口径说明**保持不动**（不再精简）→ 直接发。
- `workbuddy_sites_deploy`（directory=`D:/mywork/stock-alert-console`、**entryHtml=`console.html`**、appName=`A股监控控制台`、domainPrefix=`stock-alert-console`、`userAskedToPublish:true`、**`updateExistingApp:true`**）→ **shareLink 不变 `https://stock-alert-console.app.workbuddy.host/`**、verified=true、sandboxId=e39b0b386b3a46f4be731010375a3190。**「全上」含仓位降级块 → 线上旧版（带全量仓位块）至此也被覆盖，本地/线上重新一致。**
- 校验：线上 vs 本地 **397,818 字节 / 7,755 行 / 差异 0 行**（md5 f0b3c78e795d 一致）。
  - ⚠️ **踩坑（新）：`https.get` 里 `res.on('data', d => b += d)` 分块 toString 会把跨块的多字节汉字截成 U+FFFD**，害我一度以为线上文件被改坏（看到「监控��持仓行」）→ 必须先 `Buffer.concat` 再 `toString('utf8')`（或 `res.setEncoding('utf8')`）才能比 md5/字节。**比 md5 前先确认解码方式**。
- `accept-online.js`：**194 通过 / 0 失败 ALL GREEN**（含新加的「详情页只留结论/状态/核心指标着色」一条）。
- 最终线上形态：详情页无任何小字备注块；核心指标着色（蓝/橙/琥珀）+ 状态条；仓位降级为一行「你自己控/别全进全出」。

### 石大胜华 603026 日频回放（2026-04-28 → 05-27，19 个交易日）—— 用户要求「按最新规则、只用当天数据、不做事后合理化」
- 脚本 `_tests/sim-603026.js`（jsdom 加载 console.html → **只喂 ≤D 的数据** → 直接调 weekStage/v3Vote/psRuling/fusionVerdict/mlStopLoss/mlMarketCtx 等真函数）；关键技巧：**`w.todayStr = () => D`** 让「本态第 N 天 / 冲突计数」按回放日算；V3 三源构造照抄 `v3Ensure`（1837-1863 行）；输出落 `_tests/_cache/sim-603026-out.txt`。
- **结果（前复权）**：S2 只在 **05-06（收盘 120.36，全窗口最高收盘）** 出 1 天「高位过热·减仓（第 1 天）」，次日 05-07 即 −9.82%；其余 18 天全部「趋势运行中·无需操作」。止损位 97.53（= 首日收盘 108.37×0.9）在 **05-14 / 05-19 / 05-22 / 05-25 / 05-26 / 05-27** 收盘跌破（系统按当日现价判"是否已跌破"，不锁死，05-15 曾回到位上方）。V3：主信号 10 天 / 数据冲突·下修 9 天。恐贪 44.2→89.6（5/7~5/20 处贪婪区）。
- ⚠️ **04-29 值得记住**：收 +10.00%、一年位置 100%、偏离 MA5 9.7%，但**开盘价 106.40 < 当日 MA5 108.72 → streak5=0 → 不触发减仓**。规则按字面执行（要求连续≥2 日**开盘与收盘**都站上当日 MA5），不是事后解释。
- **数据源可用性（重要，以后做历史回放先看这张表）**：
  - 日K 前复权 ✅ `web.ifzq.gtimg.cn/appstock/app/fqkline/get?param=sh603026,day,起始,结束,700,qfq`（node 直连 200，格式 `[d,o,c,h,l,v]`，v=手）。与腾讯自选股 qfq 逐笔交叉校验 **收盘/成交量差异 0.000**（4 个日期全等）→ 该股 2026-04~09 无除权，前复权口径无歧义。
  - 恐贪 ✅ `stock-alert-cloud/fng-history.json`（`days:[[date,v,...]]`，覆盖 2023-08-15 → 2026-09-16）。
  - 流通股本：新浪 `MoneyFlow.../flzjtj` 的 `curr_capital`=23270 万股；也可用 `volume(手)×10000÷换手率` 反推（2.324~2.328 亿，稳定）。
  - ❌ **历史主力净额取不到**：东财 `push2his` 本机 socket hang up（含 1./2./7. 备用域名）；`push2delay.eastmoney.com` 只返最新 1 天（忽略 beg/end/lmt）；自选股 `data_fund_flow` 持续"服务限频"；新浪 flzjtj 只有 r0~r3（4 天）；同花顺 d.10jqka 资金流路径 404。**结论：历史窗口的 资金流/筹码 目前只能靠 chips-history.json，而该归档 2026-09-17 才建 → 2026-09-17 之前的回放必然缺这两路。** 缺失时按系统真实降级走（V3 源2=null + err「主力资金流不可得」；S2 mainRatio=NaN → 建仓区 fail-closed 不触发；综合研判走「破位待核验」）。
  - ❌ 大盘风格：`style-history.json` 仅 1 天（2026-09-19）→ 历史窗口无数据。

## 用户追问「中长线 S2 似乎准确点，这个规则是什么」（同日）
- 已按线上真代码（`console.html` L2280-2462）逐条讲清 S2：定位=中长线持仓助手、区间级波段定性、只在关键位置说话；四态 + 6 个常量（`ML_SELL_POS=0.65 / ML_SELL_MA5=0.05 / ML_SELL_STREAK=2 / ML_LOW_GATE=0.30 / ML_SUP_GATE=0.15 / ML_STOP=0.10`）；判定顺序「先风险后机会」；`topWarn`（仅多头+连站 MA5，缺位置/偏离门槛）= 只给状态条不触发卖点。
- 顺带说明"为什么看着准"：05-06 那一次命中窗口最高收盘，且 04-29（+10%、位置 100%、偏离 9.7%）被「开盘也要站上 MA5」这条硬规则正确拦下 —— 是规则按字面执行，不是事后解释。
- **MEMORY.md 已压缩**：34,500 → 16,004 字符（去重 + 删除已被 S2 取代的旧候选规则细节，细节留在当日日志与 `_tests/*.md`）。

## 持仓详情页「只留结论」精简（2026-09-20 晚，已发布）
- 用户要求：详情页只展示各引擎产出的**结论**，不展示推理过程；保留 主力(V3) + 散户行为 + 综合研判(F4) + 中长线(S2)；其余琐碎信息全撤。
- `v3DetailHtml` 现在只有：①`cd-act`「主力 → {hd.t}」+ hd.a + v3Advice ②`retailLineHtml`（cd-desc，含 `.st.{tone}` 徽章）③`cd-act`「综合研判 → {act}（{why}）」④`cd-act`「中长线 → {act}（第 N 天）」+ ws.advice ⑤`mlStopHtml`（只留结论段，删掉 3 格指标行）⑥大盘一行 cd-stat（风格/周期/恐贪 + 仅环境标注）⑦缺源 warn ⑧仓位一行 ⑨免责一行。
- **撤下**：三源明细格、多源共识/冲突天数/置信度、价格结构评分+VR+结构裁决格、S2 全部指标格（一年位置/距一年低/60日涨跌/距周级支撑压力/周量能比/周线结构/近4周主力/短期热度/筹码三格）、波段定性说明段、阶段顶部辅助段、恐贪恐慌区力度段、fusionDesc 说明。
- ⚠️ 结论文本本身（`ws.advice` 里的代价/口径数字、`mdB`）**保留**；算法层零改动（v3Vote/v3Ensure/psCalc/fusionVerdict/weekStage）。
- 测试同步改动：`position-dom-test.js` 改 20 处（三源/票数/指标格改负断言，⑨d 改判标题「第 4 天」，⑫k 改判「恐慌」，⑮e 改判 cd-v 清零，1375 改判「诱多过滤」）；`accept-online.js` 改 6 处（405 三源→四块结论 + 详情页无 cd-row/cd-v，389 三源管道改判 v3sign，443 去掉「以主力博弈为准」，448/455/471/479 去掉已撤下的指标串）。
- 结果：离线 16/16 全绿（position-dom 630/0）；发布后 `accept-online.js` **194/0**，线上本地 md5 一致（e9bc6db8f3ef）。
- 快照 `_tests/s2-snapshot.js` → `_preview/s2-market-block.html`（报告行：波段定性状态条 缺 / 着色指标格 0 个 —— 都是预期内）。

## 持仓去成本/股数 + 综合研判降级 + 首页大盘驾驶舱卡（2026-09-20 夜，已发布）
- 用户四条：①持仓详情页去掉持股成本/持仓数量，只留删除操作；②添加持仓股页无需成本/数量；③去掉风险控制信息（止损位）与「仓位你自己控」那行；④只保留 主力散户博弈(V3) + 中长线 S2 的结果；⑤首页「今日推荐」**上方**加大盘驾驶舱概览卡，点击进入驾驶舱详情。
- 追问后拍板：记账口径 **全部撤掉**（浮盈/市值/成本合计一律不显示 —— 没有成本基准就不算账）；综合研判 **降级为中性状态条**（用户原话：「它有的时候和主力博弈自相矛盾，主力博弈说在回补吸筹，综合说主力撤退」）；详情页背景的**胶囊「圆弧线框」去掉**。
- 代码改动：`position-core.js` 的 `normHolding` **去掉 `cost>0 && qty>0` 的有效性门槛**（现 cost/qty 缺省 0，只记 code+name 也算有效持仓）；console.html 撤掉 `pfBox`/汇总卡渲染调用、`hg-pnl` 浮盈行、`hg-meta` 成本股数行、持仓卡内联表单（hCost/hQty/hgSave）、`saveHolding`；统计条只报「共 N 条持仓」；`v3DetailHtml` 不再调 `mlStopHtml`/`mlPositionHtml`（**函数保留可回退**）；新增 CSS `.chip.cd-block{border:0;background:transparent;border-radius:0}`；`renderDash` 新增 ③.5 大盘驾驶舱卡（`data-act="dash-style"` → `openStyle()`，读 `mlMarketCtx()` 零额外请求）。
- 综合研判降级后形态：`cd-stat mute`「综合研判（次要参考）→ X（Y）· 以上面主力博弈为准」，不再是与主力博弈同级的彩色结论块。
- 测试：position-core-test **161/0**（normHolding 口径改判）、position-dom-test **627/0**（pfBox/表单/浮盈全改负断言）、全量离线回归 **16/16 全绿**；发布 md5 `3d965149bd2f`，accept-online **179 通过 / 0 失败 ALL GREEN**。
- ⚠️ **accept-online 5 条失败全是断言写法问题（第三次踩注释/定义误伤坑），线上无真问题**：
  ①`onNoCmt` 只剥 JS 注释 → HTML 注释里的「成本合计」仍在 ⇒ **onNoCmt 现在也剥 `<!-- -->`**。
  ②`onNoCmt.indexOf('mlStopHtml(code)')` 会命中 `function mlStopHtml(code){` 的定义本身 ⇒ 改为判**调用** `h += mlStopHtml(code)`。
  ③「驾驶舱卡在今日推荐上方」按全文件 indexOf 比顺序 → 命中首屏骨架里的 `dash-h">今日推荐`（在文件更前面）⇒ 改为**只在 `renderDash` 段内**比。
  ④「持仓浮盈」只存在于**零调用死代码 `renderPortfolio`** 里 ⇒ 改为「记账串只在该函数体内 + 全文件 renderPortfolio 出现 1 次（= 零调用方）」。
  ⑤预填断言改判监控侧 `s.mon && s.stock.condition/target`，负断言 `s.holding.cost/qty`。
- **遗留**：`renderPortfolio`（含 .pf 样式）已是死代码，下次要发版时可直接删（需同步改 accept-online 第 92/115 两条正向断言为负向）。

## 清除死代码：renderPortfolio + .pf* 整段删除并同步上线（同日深夜）
- 用户问「改成负向对结论产出展示有无影响，没有就可以操作」→ **先验证影响面再动手**：`renderPortfolio` 全文件仅剩 1 处（函数定义本身）= 零调用方；`.pf/.pf-top/.pf-lbl/.pf-val/.pf-sub/.pf-note/.pf-empty` 仅被该函数使用 → **对渲染产物零影响**（position-dom-test 627 条 DOM 断言改前改后完全一致，正好互证）。
- 删除内容（console.html）：`.pf*` 7 条 CSS + `renderPortfolio` 整个函数 + 两处过期注释（"持仓页：汇总卡（pfBox）"、"只管我真实持有什么、赚亏多少"）；保留 `priceAt`/`holdingsOf`/`posCore`（别处仍用）。
- 测试改动：accept-online 两条正向断言（`含持仓汇总卡渲染函数 renderPortfolio`、`汇总卡样式 .pf 已内嵌`）改**负向**，并把「死代码里才有记账串」的断言升级为「去注释源码里一个都不剩」；`position-snapshot.js` 修掉 `d.getElementById('pfBox').querySelector('.pf')` 崩溃（改抓统计条 `#hCount`）＋ 快照说明文案同步（原文还在讲"顶部汇总卡""三源明细/共识票数/冲突天数/置信度"，都是旧版描述）。
- 回归：全量离线 **16/16 全绿**（position-dom 627/0、console-check 29/0）；发布 md5 `898dbe801af0`，accept-online **197 通过 / 0 失败 ALL GREEN**。
- ⚠️ **同一晚两次撞同一个坑**：①`chk('监控列表不再渲染汇总卡', on.indexOf('renderPortfolio') < 0)` 漏改成 `onNoCmt` → **我在注释里写的「renderPortfolio 已整体删除」把这行自己判红了**（发布后 194/1）。教训升级为：**删代码时新写的负断言必须一开始就用 `onNoCmt`**，因为「已删除」的说明本身就会写进注释。
- ⚠️ PowerShell 抓 node stdout 会按 OEM 代码页解码 → 中文全成乱码、连 `失败` 都匹配不到（差点误判成"没有失败项"）。**抓 node 输出前先设 `[Console]::OutputEncoding = [System.Text.Encoding]::UTF8`**。

## 需求文档同步到最新版（2026-09-20 深夜）
- `股票监控系统-需求与方案说明.md` 头 "最后更新" 由 2026-09-16 → 2026-09-20；反映 9-19/20 全部改动：F26 改为只记代码+名称不记账；2.4 V3 行 Pivot 并标注 V4/V5 移出计划；2.5 落地进度新增主力博弈/S2/F4/大盘驾驶舱/持仓只留结论/死代码清理六行 + V4/V5❌；设计红线 #1/#2 标注作废；新增 2026-09-20 上线记录表 + 二·补6 口径章节；文件地图补 style.js/style-history.json 与 #pgStyle。

## 项目 git 化 + 换机/交接清单（2026-09-20 深夜，用户要求"两个都做"）
- 痛点：控制台 `stock-alert-console/` + 测试 `_tests/` + 需求文档**只在本机、未进 git**，换机会丢编辑源；`.workbuddy/memory/` 也本机独有不随项目走。
- 环境：git 不在 PATH，用绝对路径 `C:/Users/29086/.workbuddy/binaries/PortableGit/versions/1.2.0/cmd/git.exe`；bash 不认 `/d/mywork`，用相对路径 + CWD=D:\mywork 即可；`gh` 不可用（不能在本机建远程仓）。
- 动作 1（git 化）：`D:\mywork` `git init` + **白名单 `.gitignore`**（默认忽略一切，只 `!stock-alert-console/`、`!_tests/`、`!股票监控系统-需求与方案说明.md`，忽略 `_tests/_cache/`/`*.log`/`node_modules/`）；`core.autocrlf=false`（md5 比对需字节稳定）。初始提交 `b0cbcab`：仅三项入仓，个人文件零误进。
- 动作 2（文档第八节）：需求文档新增「八、换机 / 交接清单」——资产分布、三种跨机传输（整目录复制/推 GitHub/同步盘）、新机接手 5 步、记忆文件携带提醒、给新 AI 开场白模板。提交 `56e4e1d`。
- ⚠️ 远程仓未建（gh 不可用），仓库仅本机本地；travel 靠方式 A 复制 / 方式 C 同步盘 / 或用户网页建空仓后 `git remote add`+`push`（文档 8.2 已写明）。
- ⚠️ **环境核实（重要，避免以后再误判）**：本 WorkBuddy 沙盒**未注入** `GH_TOKEN`/`GITHUB_TOKEN`/`SENDKEY` 环境变量；`git`/`gh` 也不在 PATH（git 需用绝对路径 `C:/Users/29086/.workbuddy/binaries/PortableGit/versions/1.2.0/cmd/git.exe`）。**推送 GitHub 在本会话可代做**——前提是用户提供 token；沙盒代理只放行 `api.github.com`（github.com 的 git https 传输被 502 拦截），故走 **GitHub Git Data API / Contents API**（node `fetch` 直连 api.github.com）而非 `git push`。token 只经命令行 env `GH_TOKEN_PUSH` 传入，**不落盘、不写进 .git/config、不拼进 remote URL**。仓库地址 `zyhaaad/stock-alert` 在文档 §五 有记录，但那是**云端脚本仓**；控制台+测试单独建 `stock-alert-console` 仓。

## GitHub 推送完成（2026-09-20 深夜，用户发 token 后）
- 用户发 token（classic，repo 权限）→ 已建空仓 **`zyhaaad/stock-alert-console`**（public）+ 推送成功。
- 根因：沙盒代理只放行 `api.github.com`，`git push` 走 github.com 被 502；改走 **Git Data API**：blobs→tree→commit→更新 ref。
- ⚠️ **三个必须踩平的坑（脚本已固化，下次直接复用 `_tests/_cache/push-github-v2.js`）**：
  ① 空仓 Git Data API 建 blob 直接 **409「Git Repository is empty」**（不是 401，说明 token 鉴权 OK，只是仓库零提交）→ 先 `PUT /contents/README.md`（Contents API）种一棵初始化 main，再走 Git Data API 做干净提交。
  ② `git ls-files` 对**非 ASCII 文件名**（需求文档中文名）会加双引号 + 八进制转义（`"\350\202\241..."`）→ `fs.readFileSync` 拼出假路径 ENOENT → 改 `git ls-files -z`（NUL 分隔、原始 UTF-8）+ `Buffer.toString('utf8')` 解码。
  ③ 更新已存在的 ref 用 **PATCH** `/git/refs/heads/main`，不是 POST（`POST` 对已存在 ref 报 422）。
- 结果：main 三提交 = 种 README(`55485249`) + 77 文件(`31e4dd60`) + README 补回(`2526fa94`)；根目录 `README.md` / `_tests/` / `stock-alert-console/` / `股票监控系统-需求与方案说明.md` 四者齐全；remote `origin=https://github.com/zyhaaad/stock-alert-console.git` 无 token。
- 核验：递归 tree `truncated:false`、blobs 77、`has console.html:true`、`has doc:true`；中文文件名在 GitHub 正确显示（非八进制）。
- 复跑提示：以后本地改完要再同步，直接 `GH_TOKEN_PUSH=<token> node _tests/_cache/push-github-v2.js`（已能自处理「main 已存在→不重种、只追加提交」）。
  - ⚠️ **单文件更新（如只改了需求文档）别用 push-github-v2.js**：它会重建整棵树、把 README 再次挤掉。改用**外科手术式 Contents API PUT**（`_tests/_cache/update-doc.js`：GET 当前 sha → PUT 新内容+sha，只动那一个文件，README 与其余 76 文件原样保留）。2026-09-20 晚即用此法把第八节同步到 GitHub（commit `b2e7960c`，本地提交 `8a775a7`）。
  - 新 token（轮换后）已用于本次同步（值经 env 传入、不落盘、**绝不写入记忆文件**）；旧 token 建议用户吊销。token 一律经 env 传入、不落盘、不拼进 remote URL。
